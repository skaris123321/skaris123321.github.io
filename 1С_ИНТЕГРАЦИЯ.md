# Интеграция сайта с 1С: Пошаговая инструкция

---

## СОДЕРЖАНИЕ - ЧТО ДЕЛАТЬ И В КАКОМ ПОРЯДКЕ

```
ШАГ 1 → Проверка версии 1С
ШАГ 2 → Создание промежуточных таблиц (хранилище заказов)
ШАГ 3 → HTTP-сервис: ПРИЕМ ЗАКАЗОВ С САЙТА (сайт → 1С)
ШАГ 4 → Создание пользователя для веб-сервиса
ШАГ 5 → Обработка: СОЗДАНИЕ ДОКУМЕНТОВ в 1С из таблиц
ШАГ 6 → Обработка: ОБНОВЛЕНИЕ ЦЕН НА САЙТЕ (1С → сайт)
ШАГ 7 → Настройка сетевого доступа
ШАГ 8 → Проверка работы
```

### Три разных блока кода - не перепутайте!

| Блок | Где в файле | Что делает | Кто вызывает |
|------|-------------|-----------|--------------|
| **HTTP-сервис `create`** | Шаг 3 | Принимает заказ с сайта и пишет в таблицу | Сайт автоматически при оформлении заказа |
| **Обработка `ОбработатьНовыеЗаказы`** | Шаг 5 | Читает таблицу и создает документ "Заказ покупателя" | Регламентное задание каждые 5 минут |
| **Обработка `ОбновитьЦеныНаСайте`** | Шаг 6 | Берет цены из 1С и отправляет на сайт | Регламентное задание (расписание по желанию) |

---

## Что необходимо настроить

Цель — автоматический приём заказов с сайта в 1С и автоматическое обновление цен на сайте из 1С.

**Схема работы:**
1. Клиент оформляет заказ на сайте → HTTP-сервис записывает данные в промежуточные таблицы
2. Регламентное задание читает таблицы → создаёт документ «Заказ покупателя» в 1С
3. Цены из справочника Номенклатуры → по расписанию отправляются на сайт

---

## Шаг 1: Проверка версии 1С

Нужна серверная версия 1С:Предприятие 8.3 или выше.

1. Откройте 1С
2. Справка → О программе
3. Должно быть написано "1С:Предприятие 8.3 (сервер)" или выше
4. Если файловая версия - нужна серверная

---

## Шаг 2: Создание промежуточных таблиц

⚠️ **ВАЖНО! СНАЧАЛА ПРОВЕРЬТЕ СУЩЕСТВУЮЩИЕ ТАБЛИЦЫ!**

Если у вас уже есть таблицы для импорта заказов с сайта, **ОБЯЗАТЕЛЬНО** проверьте их структуру!

**Как проверить:**
1. Откройте конфигуратор 1С
2. Найдите существующие таблицы (например: "ВнешниеЗаказы", "ЗаказыСайта", "WebOrders" и т.п.)
3. Посмотрите какие поля там есть
4. Если структура отличается от описанной ниже — пожалуйста, сообщите нам

**Почему это важно:**
- Если таблицы уже существуют, но с другими полями — интеграция не будет работать корректно
- В этом случае необходимо либо адаптировать код под существующие таблицы, либо создать новые с правильной структурой

---

### Если таблиц нет — создайте новые:

Создайте две таблицы для хранения данных заказов с сайта.

### Таблица 1: WebOrdersImport (основные данные заказа)

Создайте таблицу с полями:

| Поле | Тип | Описание |
|------|-----|---------|
| OrderID | Строка | Уникальный ID заказа |
| ClientName | Строка | Название компании |
| ClientPhone | Строка | Телефон |
| ClientEmail | Строка | Email |
| OrderTotal | Число | Сумма заказа |
| OrderDate | Дата | Дата создания |
| Status | Строка | Статус: "Новый", "Обработан", "Ошибка" |

### Таблица 2: WebOrderItemsImport (товары в заказе)

Создайте таблицу с полями:

| Поле | Тип | Описание |
|------|-----|---------|
| OrderID | Строка | Связь с заказом |
| Article | Строка | Артикул товара |
| ItemName | Строка | Название товара |
| Quantity | Число | Количество |
| Price | Число | Цена за единицу |

---

### Если таблицы уже существуют — сообщите их структуру:

Пожалуйста, напишите список полей в формате:

```
Таблица: [название]
Поля:
- [название поля 1] - [тип]
- [название поля 2] - [тип]
...
```

Я адаптирую код под вашу структуру.

---

## Шаг 3: Создание HTTP-сервиса для приема заказов

> 📥 **ЭТОТ КОД ПРИНИМАЕТ ЗАКАЗЫ С САЙТА**
> Сайт сам вызывает этот сервис когда клиент нажимает "Оформить заказ".
> Вам не нужно ничего запускать вручную - он работает автоматически.

### Создание сервиса в 1С

1. Конфигуратор 1С
2. Общие → Web-сервисы → HTTP-сервисы
3. Создать новый HTTP-сервис: `orders`
4. Добавить URL-шаблон: `create` (метод POST)

### Код обработчика

> ⬇️ **КОД №1: HTTP-СЕРВИС (ПРИЕМ ЗАКАЗОВ С САЙТА)**
> Скопируйте этот код в обработчик метода `create` HTTP-сервиса

```1c
Функция create(Запрос)
	
	Попытка
		
		// Получаем JSON от сайта
		ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку();
		
		// Парсим JSON
		ЧтениеJSON = Новый ЧтениеJSON();
		ЧтениеJSON.УстановитьСтроку(ТелоЗапроса);
		ДанныеЗаказа = ПрочитатьJSON(ЧтениеJSON);
		
		// Извлекаем данные клиента
		// ⚠️ МЕНЯТЬ НЕ НУЖНО - эти ключи фиксированы со стороны сайта
		ДанныеКлиента = ДанныеЗаказа["client"];
		ИмяКлиента    = ДанныеКлиента["name"];
		ТелефонКлиента = ДанныеКлиента["phone"];
		EmailКлиента  = ДанныеКлиента["email"];
		
		// Извлекаем товары и сумму
		// ⚠️ МЕНЯТЬ НЕ НУЖНО - эти ключи фиксированы со стороны сайта
		ТоварыЗаказа = ДанныеЗаказа["items"];
		СуммаЗаказа  = ДанныеЗаказа["total"];
		
		// Генерируем уникальный ID заказа
		IDЗаказа = Строка(Новый УникальныйИдентификатор());
		
		// Создаем запись в таблице WebOrdersImport
		// ⚠️ ЗАМЕНИТЬ: "WebOrdersImport" → название вашей таблицы если она уже существует
		// ⚠️ ЗАМЕНИТЬ: названия полей (OrderID, ClientName и т.д.) → если у вас другие названия полей
		НоваяЗапись = РегистрыСведений.WebOrdersImport.СоздатьМенеджерЗаписи();
		НоваяЗапись.OrderID    = IDЗаказа;
		НоваяЗапись.ClientName  = ИмяКлиента;
		НоваяЗапись.ClientPhone = ТелефонКлиента;
		НоваяЗапись.ClientEmail = EmailКлиента;
		НоваяЗапись.OrderTotal  = СуммаЗаказа;
		НоваяЗапись.OrderDate   = ТекущаяДата();
		НоваяЗапись.Status      = "Новый"; // ⚠️ ЗАМЕНИТЬ если статусы называются иначе в вашей конфигурации
		НоваяЗапись.Записать();
		
		// Создаем записи товаров
		// ⚠️ ЗАМЕНИТЬ: "WebOrderItemsImport" → название вашей таблицы товаров если она уже существует
		// ⚠️ ЗАМЕНИТЬ: названия полей → если у вас другие названия полей
		Для каждого Товар Из ТоварыЗаказа Цикл
			
			// ⚠️ МЕНЯТЬ НЕ НУЖНО - эти ключи фиксированы со стороны сайта
			Артикул       = Товар["article"];
			НазваниеТовара = Товар["name"];
			Количество    = Товар["quantity"];
			Цена          = Товар["price"];
			
			НоваяСтрока = РегистрыСведений.WebOrderItemsImport.СоздатьМенеджерЗаписи();
			НоваяСтрока.OrderID   = IDЗаказа;
			НоваяСтрока.Article   = Артикул;
			НоваяСтрока.ItemName  = НазваниеТовара;
			НоваяСтрока.Quantity  = Количество;
			НоваяСтрока.Price     = Цена;
			НоваяСтрока.Записать();
			
		КонецЦикла;
		
		// Возвращаем успешный ответ сайту
		// ⚠️ МЕНЯТЬ НЕ НУЖНО
		Ответ = Новый Структура("success,orderID,message", Истина, IDЗаказа, "Заказ успешно принят");
		ЗаписьJSON = Новый ЗаписьJSON();
		ЗаписатьJSON(ЗаписьJSON, Ответ);
		Возврат ЗаписьJSON.Закрыть();
		
	Исключение
		
		Ответ = Новый Структура("success,error", Ложь, ОписаниеОшибки());
		ЗаписьJSON = Новый ЗаписьJSON();
		ЗаписатьJSON(ЗаписьJSON, Ответ);
		Возврат ЗаписьJSON.Закрыть();
		
	КонецПопытки;
	
КонецФункции
```

### Формат JSON от сайта

Сайт будет отправлять данные в этом формате:

```json
{
  "client": {
    "name": "ООО Стройка",
    "phone": "+7 (495) 123-45-67",
    "email": "zakaz@stroyka.ru"
  },
  "items": [
    {
      "article": "AVR-100A",
      "name": "АВР 100А на 2 ввода",
      "quantity": 2,
      "price": 45000
    }
  ],
  "total": 90000
}
```

---

## Шаг 4: Создание пользователя для веб-сервиса

1. В 1С: Администрирование → Пользователи
2. Создать нового пользователя: `WebService`
3. Установить пароль (запомнить для сайта)
4. Назначить права:
   - Создание документов "Заказ покупателя"
   - Чтение справочников "Контрагенты" и "Номенклатура"
   - Доступ к HTTP-сервисам

---

## Шаг 5: Создание обработки для автоматического создания заказов

> 📤 **ЭТОТ КОД СОЗДАЕТ ДОКУМЕНТЫ В 1С**
> Читает новые заказы из промежуточной таблицы и создает документ "Заказ покупателя".
> Запускается автоматически регламентным заданием каждые 5 минут.

Эта обработка будет автоматически читать новые заказы из таблицы и создавать документы.

### Что нужно заполнить в документе

**Шапка документа (обязательные поля):**
- Контрагент (найти по ClientName)
- Организация (ваша организация)
- Договор (договор с контрагентом)
- Менеджер (кто создал заказ)

**Табличная часть (для каждого товара):**
- Номенклатура (найти по Article)
- Количество (из Quantity)
- Цена (из Price)
- Единица измерения

### Примерный код обработки

> ⬇️ **КОД №2: ОБРАБОТКА (СОЗДАНИЕ ДОКУМЕНТОВ В 1С)**
> Скопируйте этот код в обработку, которую будет вызывать регламентное задание

```1c
Процедура ОбработатьНовыеЗаказы()
	
	// Получаем все новые заказы
	Запрос = Новый Запрос();
	Запрос.Текст = "
	|ВЫБРАТЬ
	|	WebOrdersImport.OrderID,
	|	WebOrdersImport.ClientName,
	|	WebOrdersImport.ClientPhone,
	|	WebOrdersImport.ClientEmail,
	|	WebOrdersImport.OrderTotal,
	|	WebOrdersImport.OrderDate
	|ИЗ
	|	WebOrdersImport
	|ГДЕ
	|	WebOrdersImport.Status = 'Новый'
	";
	
	РезультатЗапроса = Запрос.Выполнить();
	ВыборкаЗаказа = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаЗаказа.Следующий() Цикл
		
		Попытка
			
			// Создаем новый документ "Заказ покупателя"
			ДокументЗаказ = Документы.ЗаказПокупателя.СоздатьДокумент();
			
			// Заполняем дату
			ДокументЗаказ.Дата = ВыборкаЗаказа.OrderDate;
			
			// Ищем контрагента по названию
			КонтрагентЗапрос = Новый Запрос();
			КонтрагентЗапрос.Текст = "
			|ВЫБРАТЬ
			|	Контрагенты.Ссылка
			|ИЗ
			|	Справочник.Контрагенты КАК Контрагенты
			|ГДЕ
			|	Контрагенты.Наименование = :Наименование
			|	И Контрагенты.ПометкаУдаления = ЛОЖЬ
			";
			КонтрагентЗапрос.УстановитьПараметр("Наименование", ВыборкаЗаказа.ClientName);
			
			РезультатКонтрагента = КонтрагентЗапрос.Выполнить();
			ВыборкаКонтрагента = РезультатКонтрагента.Выбрать();
			
			Если ВыборкаКонтрагента.Следующий() Тогда
				ДокументЗаказ.Контрагент = ВыборкаКонтрагента.Ссылка;
			Иначе
				// Если контрагента нет - пропускаем заказ
				ОбновитьЗапрос = Новый Запрос();
				ОбновитьЗапрос.Текст = "
				|ОБНОВИТЬ WebOrdersImport
				|УСТАНОВИТЬ Status = 'Ошибка'
				|ГДЕ
				|	OrderID = :OrderID
				";
				ОбновитьЗапрос.УстановитьПараметр("OrderID", ВыборкаЗаказа.OrderID);
				ОбновитьЗапрос.Выполнить();
				Продолжить;
			КонецЕсли;
			
			// Заполняем остальные обязательные поля
			// АДАПТИРУЙТЕ ЭТИ СТРОКИ ПОД ВАШУ КОНФИГУРАЦИЮ
			ДокументЗаказ.Организация = ПолучитьОрганизацию(); // ваша функция
			ДокументЗаказ.Договор = ПолучитьДоговор(ДокументЗаказ.Контрагент); // ваша функция
			ДокументЗаказ.Менеджер = ПолучитьМенеджера(); // ваша функция
			
			// Получаем товары заказа
			ТоварыЗапрос = Новый Запрос();
			ТоварыЗапрос.Текст = "
			|ВЫБРАТЬ
			|	WebOrderItemsImport.Article,
			|	WebOrderItemsImport.ItemName,
			|	WebOrderItemsImport.Quantity,
			|	WebOrderItemsImport.Price
			|ИЗ
			|	WebOrderItemsImport
			|ГДЕ
			|	WebOrderItemsImport.OrderID = :OrderID
			";
			ТоварыЗапрос.УстановитьПараметр("OrderID", ВыборкаЗаказа.OrderID);
			
			РезультатТоваров = ТоварыЗапрос.Выполнить();
			ВыборкаТовара = РезультатТоваров.Выбрать();
			
			// Добавляем товары в документ
			Пока ВыборкаТовара.Следующий() Цикл
				
				// Ищем товар по артикулу
				ТоварЗапрос = Новый Запрос();
				ТоварЗапрос.Текст = "
				|ВЫБРАТЬ
				|	Номенклатура.Ссылка,
				|	Номенклатура.ЕдиницаИзмерения
				|ИЗ
				|	Справочник.Номенклатура КАК Номенклатура
				|ГДЕ
				|	Номенклатура.Артикул = :Артикул
				|	И Номенклатура.ПометкаУдаления = ЛОЖЬ
				";
				ТоварЗапрос.УстановитьПараметр("Артикул", ВыборкаТовара.Article);
				
				РезультатТовара = ТоварЗапрос.Выполнить();
				ВыборкаНайденногоТовара = РезультатТовара.Выбрать();
				
				Если ВыборкаНайденногоТовара.Следующий() Тогда
					
					// Добавляем строку в табличную часть
					НоваяСтрока = ДокументЗаказ.Товары.Добавить();
					НоваяСтрока.Номенклатура = ВыборкаНайденногоТовара.Ссылка;
					НоваяСтрока.Количество = ВыборкаТовара.Quantity;
					НоваяСтрока.Цена = ВыборкаТовара.Price;
					НоваяСтрока.ЕдиницаИзмерения = ВыборкаНайденногоТовара.ЕдиницаИзмерения;
					НоваяСтрока.Сумма = ВыборкаТовара.Quantity * ВыборкаТовара.Price;
					
				КонецЕсли;
				
			КонецЦикла;
			
			// Сохраняем документ
			ДокументЗаказ.Записать();
			
			// Если нужно - пытаемся провести
			// ДокументЗаказ.Провести();
			
			// Обновляем статус в промежуточной таблице
			ОбновитьЗапрос = Новый Запрос();
			ОбновитьЗапрос.Текст = "
			|ОБНОВИТЬ WebOrdersImport
			|УСТАНОВИТЬ Status = 'Обработан'
			|ГДЕ
			|	OrderID = :OrderID
			";
			ОбновитьЗапрос.УстановитьПараметр("OrderID", ВыборкаЗаказа.OrderID);
			ОбновитьЗапрос.Выполнить();
			
		Исключение
			
			// Обновляем статус на "Ошибка"
			ОбновитьЗапрос = Новый Запрос();
			ОбновитьЗапрос.Текст = "
			|ОБНОВИТЬ WebOrdersImport
			|УСТАНОВИТЬ Status = 'Ошибка'
			|ГДЕ
			|	OrderID = :OrderID
			";
			ОбновитьЗапрос.УстановитьПараметр("OrderID", ВыборкаЗаказа.OrderID);
			ОбновитьЗапрос.Выполнить();
			
		КонецПопытки;
		
	КонецЦикла;
	
КонецПроцедуры
```

### Как использовать обработку

1. Адаптируйте код под вашу конфигурацию (названия полей, справочников)
2. Создайте регламентное задание "Обработка заказов с сайта"
3. Установите периодичность: каждые 5 минут
4. В обработчике вызовите: `ОбработатьНовыеЗаказы()`

---

## Шаг 6: Создание обработки для обновления цен на сайте

> 💰 **ЭТОТ КОД ОТПРАВЛЯЕТ ЦЕНЫ ИЗ 1С НА САЙТ**
> Берет актуальные цены из справочника Номенклатуры и отправляет на сайт автоматически.
> Запускается регламентным заданием по расписанию (например, каждую ночь или раз в час).

### Как это работает

Сайт уже имеет готовый адрес для приёма цен:
```
https://nku.rosek.ru/api/update-prices.php
```
Этот адрес никуда не нужно "брать" — он уже существует на сервере сайта.
1С-ник просто вписывает его в код ниже и отправляет туда цены.

### Код обработки

> ⬇️ **КОД №3: ОБРАБОТКА (ОБНОВЛЕНИЕ ЦЕН НА САЙТЕ)**
> Скопируйте этот код в обработку, которую будет вызывать регламентное задание

```1c
Процедура ОбновитьЦеныНаСайте()
	
	// URL сайта — менять не нужно, он уже правильный
	URLСайта = "https://nku.rosek.ru/api/update-prices.php";
	
	// Пароль для доступа к сайту — уже вписан ниже, менять не нужно
	APIТокен = "rosek-secret-token-2026";
	
	Попытка
		
		// Получаем все товары из справочника Номенклатуры
		Запрос = Новый Запрос();
		Запрос.Текст = "
		|ВЫБРАТЬ
		|	Номенклатура.Артикул КАК Артикул,
		|	ЦеныНоменклатуры.Цена КАК Цена
		|ИЗ
		|	Справочник.Номенклатура КАК Номенклатура
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ЦеныНоменклатуры.СрезПоследних(
		|			,
		|			// ⚠️ ЗАМЕНИТЬ: укажите нужный тип цены (например: ВидЦены = &ВидЦены)
		|			ВидЦены = &ВидЦены
		|		) КАК ЦеныНоменклатуры
		|		ПО Номенклатура.Ссылка = ЦеныНоменклатуры.Номенклатура
		|ГДЕ
		|	Номенклатура.ПометкаУдаления = ЛОЖЬ
		|	И Номенклатура.Артикул <> ''
		|	И ЦеныНоменклатуры.Цена > 0
		";
		
		// ⚠️ ЗАМЕНИТЬ: укажите нужный вид цены из вашей конфигурации
		Запрос.УстановитьПараметр("ВидЦены", Справочники.ВидыЦен.НайтиПоНаименованию("Розничная"));
		
		РезультатЗапроса = Запрос.Выполнить();
		Выборка = РезультатЗапроса.Выбрать();
		
		// Формируем массив цен для отправки
		МассивЦен = Новый Массив();
		
		Пока Выборка.Следующий() Цикл
			
			Если ПустаяСтрока(Выборка.Артикул) Тогда
				Продолжить;
			КонецЕсли;
			
			ЗаписьЦены = Новый Структура("article,price");
			ЗаписьЦены.article = Выборка.Артикул;
			ЗаписьЦены.price   = Выборка.Цена;
			МассивЦен.Добавить(ЗаписьЦены);
			
		КонецЦикла;
		
		Если МассивЦен.Количество() = 0 Тогда
			Сообщить("Нет цен для отправки");
			Возврат;
		КонецЕсли;
		
		// Формируем JSON
		ДанныеДляОтправки = Новый Структура("prices", МассивЦен);
		ЗаписьJSON = Новый ЗаписьJSON();
		ЗаписьJSON.УстановитьСтроку();
		ЗаписатьJSON(ЗаписьJSON, ДанныеДляОтправки);
		ТелоЗапроса = ЗаписьJSON.Закрыть();
		
		// Отправляем на сайт
		HTTPСоединение = Новый HTTPСоединение(
			"nku.rosek.ru",
			443,
			,
			,
			,
			30, // таймаут 30 секунд
			Новый ЗащищенноеСоединениеOpenSSL()
		);
		
		Заголовки = Новый Соответствие();
		Заголовки["Content-Type"]  = "application/json";
		// Пароль — менять не нужно
		Заголовки["Authorization"] = "Bearer " + APIТокен;
		
		HTTPЗапрос = Новый HTTPЗапрос("/api/update-prices.php", Заголовки);
		HTTPЗапрос.УстановитьТелоИзСтроки(ТелоЗапроса);
		
		HTTPОтвет = HTTPСоединение.ОтправитьДляОбработки(HTTPЗапрос);
		
		// Проверяем ответ
		Если HTTPОтвет.КодСостояния = 200 Тогда
			
			ЧтениеJSON = Новый ЧтениеJSON();
			ЧтениеJSON.УстановитьСтроку(HTTPОтвет.ПолучитьТелоКакСтроку());
			Ответ = ПрочитатьJSON(ЧтениеJSON);
			
			Сообщить("Цены обновлены: " + Ответ["updated"] + " товаров");
			
			Если Ответ["notFound"].Количество() > 0 Тогда
				Сообщить("Не найдены на сайте: " + СтрСоединить(Ответ["notFound"], ", "));
			КонецЕсли;
			
		Иначе
			Сообщить("Ошибка обновления цен. Код: " + HTTPОтвет.КодСостояния);
		КонецЕсли;
		
	Исключение
		Сообщить("Ошибка при обновлении цен: " + ОписаниеОшибки());
	КонецПопытки;
	
КонецПроцедуры
```

### Как использовать

1. URL и токен уже вписаны в код выше — ничего менять не нужно
2. Адаптируйте запрос под вашу конфигурацию (вид цены, название регистра цен)
3. Создайте регламентное задание "Обновление цен на сайте"
4. Установите расписание (например: каждый день в 8:00, или каждый час)
5. В обработчике вызовите: `ОбновитьЦеныНаСайте()`

---

## Шаг 7: Настройка сетевого доступа

### На сервере 1С

1. Командная строка (администратор): `ipconfig`
2. Запишите IPv4-адрес (например: 192.168.1.100)
3. Панель управления → Брандмауэр Windows
4. Создать правило для входящих подключений на порт 8080

### На роутере офиса

1. Зайти в настройки роутера (обычно 192.168.1.1)
2. Найти "Проброс портов" или "Port Forwarding"
3. Настроить:
   - Внешний порт: 8080
   - Внутренний IP: [IP сервера 1С]
   - Внутренний порт: 8080
4. Узнать внешний IP на whatismyipaddress.com

---

## Шаг 8: Проверка работы

### Проверить HTTP-сервисы локально

На сервере 1С откройте браузер и зайдите на:
```
http://localhost:8080/ваша_база/hs/
```

Должен показать список доступных сервисов.

### Проверить доступ из интернета

```
http://внешний-IP:8080/ваша_база/hs/orders/create
```

### Тестовый запрос заказа

```bash
curl -X POST http://внешний-IP:8080/ваша_база/hs/orders/create \
  -H "Content-Type: application/json" \
  -u WebService:пароль \
  -d '{
    "client": {
      "name": "Тестовая компания",
      "phone": "+7 (495) 123-45-67",
      "email": "test@example.com"
    },
    "items": [
      {
        "article": "AVR-100A",
        "name": "АВР 100А",
        "quantity": 1,
        "price": 45000
      }
    ],
    "total": 45000
  }'
```

---

## Итоговые данные для внесения в config.ini

После завершения настройки необходимо внести следующие данные в файл `config.ini` на сервере сайта:

```
ONEC_API_URL = "http://ваш-внешний-IP:8080/база/hs/orders/create"
ONEC_USERNAME = "WebService"
ONEC_PASSWORD = "установленный пароль"
```

Эти данные предоставляет программист 1С после завершения настройки.

---

## Что дальше

После успешной настройки процесс работает так:
1. Клиент заполняет форму на сайте и нажимает "Оформить заказ"
2. Сайт автоматически отправляет заказ в 1С через HTTP-сервис
3. Заказ сохраняется в промежуточных таблицах
4. Регламентное задание каждые 5 минут создаёт документ "Заказ покупателя"
5. Менеджер видит заказ в 1С и начинает работу с клиентом
6. Цены на сайте обновляются автоматически по расписанию из 1С
