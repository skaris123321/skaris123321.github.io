# Мои действия по интеграции с 1С

---

## Что нужно сделать мне (не программисту 1С)

---

## Шаг 1: Заказать VDS у хостинга

Написать в поддержку хостинга:

```
Добрый день!

Нам необходим VDS для интеграции сайта с 1С.

Требования:
1. VDS с Ubuntu 22.04
2. PHP 7.4+
3. 2 GB RAM, 20 GB диск
4. Поддомен nku.rosek.ru → IP VDS
5. SSL-сертификат для nku.rosek.ru
6. Открытые HTTP/HTTPS порты
7. Доступ по SSH или FTP
```

Получить от хостинга:
- IP адрес VDS
- Логин и пароль
- Подтверждение настройки поддомена и SSL

---

## Шаг 2: Отправить программисту 1С файл `1С_ИНТЕГРАЦИЯ.md`

Написать ему:

```
Добрый день!

Прошу выполнить интеграцию нашего сайта с 1С.
Техническое задание — в приложенном файле 1С_ИНТЕГРАЦИЯ.md

Когда будет готово, пожалуйста, передайте:
- Внешний IP сервера 1С
- Порт (обычно 8080)
- Логин и пароль пользователя WebService
```

---

## Шаг 3: Загрузить файлы на VDS (после получения доступа от хостинга)

Загрузить через FTP в папку `/var/www/nku.rosek.ru/`:

- `api/orders.php`
- `api/health.php`
- `api/update-prices.php`
- `config.ini`
- папку `orders/` (пустую)

---

## Шаг 4: Заполнить config.ini (после получения данных от программиста 1С)

Открыть файл `config.ini` и вписать данные которые передал программист 1С:

```ini
ONEC_API_URL = "http://[IP от программиста]:8080/[база]/hs/orders/create"
ONEC_USERNAME = "WebService"
ONEC_PASSWORD = "[пароль от программиста]"
```

Остальное в файле не трогать.

---

## Шаг 5: Проверка

Открыть в браузере:
```
https://nku.rosek.ru/api/health.php
```

Должно появиться:
```json
{"status": "ok"}
```

Если появилось — всё работает. Можно делать тестовый заказ на сайте и проверять что он появился в 1С.

---

## Как работает процесс после запуска

1. Клиент выбирает товары, заполняет форму и нажимает «Оформить заказ»
2. Сайт автоматически отправляет заказ в 1С
3. В 1С создаётся документ «Заказ покупателя»
4. Менеджер видит заказ и начинает работу с клиентом
5. Цены на сайте обновляются автоматически по расписанию из 1С

---

## Порядок действий

| Шаг | Что делаю | Жду от кого |
|-----|-----------|-------------|
| 1 | Пишу хостингу | Доступы к VDS |
| 2 | Отправляю файл программисту 1С | Данные 1С (IP, порт, пароль) |
| 3 | Загружаю файлы на VDS | — |
| 4 | Вписываю данные в config.ini | — |
| 5 | Проверяю работу | — |

Шаги 1 и 2 можно делать одновременно.
