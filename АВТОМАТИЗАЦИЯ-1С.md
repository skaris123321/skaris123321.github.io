# Автоматизация обновления цен из 1С

## 🎯 Цель

Программист 1С настраивает автоматическую отправку цен на сайт без вашего участия.

## 📋 Что нужно сделать

### Шаг 1: Получить постоянный URL

**Вариант А: Через ngrok (бесплатно)**

1. Зарегистрируйтесь на https://ngrok.com/
2. Получите authtoken
3. Настройте постоянный домен (например: `rosek-prices.ngrok.io`)

**Вариант Б: Разместить сервер на хостинге**

Разместите backend на отдельном сервере с постоянным адресом:
```
https://api.rosek.tech
```

### Шаг 2: Дать программисту 1С URL

Отправьте программисту:

```
URL для обновления цен:
POST https://rosek-prices.ngrok.io/api/products/update-prices

Формат данных:
{
  "prices": [
    {"article": "АВР-2-32-М-РОСЭК", "price": 46947},
    {"article": "АВР-2-40-М-РОСЭК", "price": 46947}
  ]
}

Headers:
Content-Type: application/json; charset=utf-8
```

### Шаг 3: Программист настраивает автоотправку в 1С

**Пример кода для 1С:**

```1c
// Процедура автоматического обновления цен на сайте
// Вызывается по расписанию (например, каждый день в 9:00)

Процедура АвтоОбновлениеЦенНаСайте() Экспорт
    
    // URL сервера (получен от администратора сайта)
    АдресСервера = "https://rosek-prices.ngrok.io";
    
    // Формируем массив цен
    МассивЦен = Новый Массив;
    
    // Получаем товары из 1С
    Запрос = Новый Запрос;
    Запрос.Текст = "
    |ВЫБРАТЬ
    |    Номенклатура.Артикул КАК article,
    |    Номенклатура.Цена КАК price
    |ИЗ
    |    Справочник.Номенклатура КАК Номенклатура
    |ГДЕ
    |    Номенклатура.ВыгружатьНаСайт = ИСТИНА
    |    И Номенклатура.ЦенаИзменена = ИСТИНА
    |";
    
    Результат = Запрос.Выполнить();
    Выборка = Результат.Выбрать();
    
    Пока Выборка.Следующий() Цикл
        Цена = Новый Структура;
        Цена.Вставить("article", Выборка.article);
        Цена.Вставить("price", Выборка.price);
        МассивЦен.Добавить(Цена);
    КонецЦикла;
    
    Если МассивЦен.Количество() = 0 Тогда
        Сообщить("Нет измененных цен для выгрузки");
        Возврат;
    КонецЕсли;
    
    // Формируем JSON
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    
    Данные = Новый Структура;
    Данные.Вставить("prices", МассивЦен);
    
    ЗаписатьJSON(ЗаписьJSON, Данные);
    ТелоЗапроса = ЗаписьJSON.Закрыть();
    
    // Отправляем запрос
    Попытка
        HTTPЗапрос = Новый HTTPЗапрос("/api/products/update-prices");
        HTTPЗапрос.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
        HTTPЗапрос.УстановитьТелоИзСтроки(ТелоЗапроса, КодировкаТекста.UTF8);
        
        Соединение = Новый HTTPСоединение(АдресСервера);
        Ответ = Соединение.ОтправитьДляОбработки(HTTPЗапрос);
        
        Если Ответ.КодСостояния = 200 Тогда
            // Разбираем ответ
            ЧтениеJSON = Новый ЧтениеJSON;
            ЧтениеJSON.УстановитьСтроку(Ответ.ПолучитьТелоКакСтроку());
            Результат = ПрочитатьJSON(ЧтениеJSON);
            ЧтениеJSON.Закрыть();
            
            Сообщить("✅ Обновлено цен на сайте: " + Результат.updated);
            
            // Сбрасываем флаг изменения цен
            Для Каждого Товар Из МассивЦен Цикл
                СброситьФлагИзмененияЦены(Товар.article);
            КонецЦикла;
            
            Если Результат.notFound.Количество() > 0 Тогда
                Сообщить("⚠️ Не найдены артикулы: " + СтрСоединить(Результат.notFound, ", "));
            КонецЕсли;
        Иначе
            ВызватьИсключение("Ошибка HTTP: " + Ответ.КодСостояния);
        КонецЕсли;
        
    Исключение
        Сообщить("❌ Ошибка обновления цен на сайте: " + ОписаниеОшибки());
        // Можно отправить уведомление администратору
    КонецПопытки;
    
КонецПроцедуры

// Вспомогательная функция
Процедура СброситьФлагИзмененияЦены(Артикул)
    // Реализация зависит от вашей структуры 1С
КонецПроцедуры
```

### Шаг 4: Настроить расписание в 1С

В 1С создать регламентное задание:
- **Имя:** Обновление цен на сайте
- **Метод:** АвтоОбновлениеЦенНаСайте
- **Расписание:** Каждый день в 9:00 (или по вашему выбору)

## 🔄 Как это работает

```
1. Цены меняются в 1С
   ↓
2. Регламентное задание запускается (9:00)
   ↓
3. 1С автоматически отправляет цены на сайт
   ↓
4. Сайт обновляет цены
   ↓
5. Вы получаете уведомление (опционально)
```

## ✅ Преимущества

- ✅ Полностью автоматически
- ✅ Не требует вашего участия
- ✅ Цены всегда актуальные
- ✅ Логи в 1С для контроля

## 📊 Мониторинг

### В 1С
Смотрите логи регламентного задания

### На сервере
Логи в консоли:
```
📦 Получен запрос на обновление цен: 2 товаров
✅ АВР-2-32-М-РОСЭК: 48000 → 46947 ₽
✅ АВР-2-40-М-РОСЭК: 48000 → 46947 ₽
💾 Сохранен файл: data/products-avr.json
```

## 🔒 Безопасность

### Опция 1: Добавить API ключ

Обновите `server/server.js`:

```javascript
// Middleware для проверки API ключа
app.use('/api/products/update-prices', (req, res, next) => {
  const apiKey = req.headers['x-api-key'];
  
  if (apiKey !== process.env.API_KEY_1C) {
    return res.status(401).json({
      success: false,
      error: 'Неверный API ключ'
    });
  }
  
  next();
});
```

Добавьте в `.env`:
```
API_KEY_1C=ваш_секретный_ключ_12345
```

Программист 1С добавит в запрос:
```1c
HTTPЗапрос.Заголовки.Вставить("X-API-Key", "ваш_секретный_ключ_12345");
```

### Опция 2: IP whitelist

Разрешить запросы только с IP адреса офиса 1С.

## 📞 Контакты

При настройке обращайтесь:
- Email: enogovicina167@gmail.com
- Телефон: 8 (800) 55-11-052
