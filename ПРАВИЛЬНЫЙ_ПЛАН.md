# План интеграции сайта с 1С - пошаговая инструкция

## Как это работает (простыми словами)

```
1. Клиент оформляет заказ на сайте
2. Сайт сохраняет заказ в файл на сервере
3. 1С каждые 5 минут проверяет - есть ли новые заказы
4. 1С скачивает новые заказы и создает документы
```

---

## ШАГ 1: ВЫ → Хостинг-провайдер (ПЕРВЫМ ДЕЛОМ!)

**Срок: 1-2 дня**

### Что написать хостингу:

```
Добрый день!

Нужен VDS для интеграции сайта rosek.ru с 1С.

Требования:
1. VDS с Ubuntu 22.04 (или любой Linux)
2. PHP 7.4 или новее
3. 2GB RAM, 20GB диск
4. Настроить поддомен: api.rosek.ru (направить на IP VDS)
5. Выпустить бесплатный SSL-сертификат для api.rosek.ru
6. Открыть исходящие HTTP/HTTPS порты (для запросов к 1С)
7. Предоставить доступ: SSH или FTP

Спасибо!
```

### Что вы получите от хостинга:

- ✅ IP адрес VDS (например: 123.45.67.89)
- ✅ Логин и пароль для доступа (SSH или FTP)
- ✅ Подтверждение, что поддомен api.rosek.ru настроен
- ✅ Подтверждение, что SSL работает

---

## ШАГ 2: ВЫ → Программист 1С (ОДНОВРЕМЕННО С ШАГОМ 1)

**Срок: 3-5 дней**

### Что отправить программисту:

Отправьте ему файл **`1С_ИНТЕГРАЦИЯ.md`** и напишите:

```
Привет!

Нужно настроить автоматический прием заказов с сайта в 1С.

Техническое задание в прикрепленном файле 1С_ИНТЕГРАЦИЯ.md

Основная задача:
1. Создать HTTP-сервис в 1С
2. Настроить промежуточные таблицы
3. Написать обработку для создания заказов
4. Настроить автоматическую проверку каждые 5 минут

Когда будет готово, сообщи:
- IP адрес сервера 1С
- Порт HTTP-сервиса
- Логин и пароль для доступа
```

### Что должен сделать программист 1С:

**2.1. Создать HTTP-сервис в 1С:**
- URL: `http://ip_1c:порт/rosek/hs/orders`
- Методы: GET (получить список заказов), POST (подтвердить обработку)

**2.2. Создать таблицы в базе 1С:**
- Таблица "ВнешниеЗаказы" (для хранения заказов с сайта)
- Таблица "СтатусыОбработки" (для отслеживания статусов)

**2.3. Написать обработку:**
- Запрашивает у сервера список новых заказов
- Скачивает JSON файлы
- Парсит данные
- Создает документ "Заказ покупателя"
- Отправляет подтверждение на сервер

**2.4. Настроить регламентное задание:**
- Запускается каждые 5-10 минут
- Автоматически обрабатывает новые заказы

### Что вы получите от программиста:

- ✅ IP адрес 1С (например: 192.168.1.100)
- ✅ Порт (например: 8080)
- ✅ Логин и пароль для HTTP-сервиса
- ✅ Подтверждение, что все настроено

---

## ШАГ 3: ВЫ → Настройка сервера (ПОСЛЕ ШАГА 1)

**Срок: 30-60 минут**

### Когда хостинг предоставит VDS:

**3.1. Загрузить файлы на сервер**

Через FTP или SSH загрузите в папку `/var/www/api.rosek.ru/`:

```
/var/www/api.rosek.ru/
├── api/
│   ├── orders.php       ← из вашего проекта
│   ├── health.php       ← из вашего проекта
│   └── update-prices.php ← из вашего проекта
├── orders/              ← создать пустую папку
└── config.ini           ← из вашего проекта
```

**3.2. Настроить config.ini**

Откройте файл `config.ini` и впишите данные от программиста 1С:

```ini
; URL HTTP-сервиса 1С (получите от программиста)
ONEC_API_URL = "http://192.168.1.100:8080/rosek/hs/orders/create"

; Логин и пароль (получите от программиста)
ONEC_USERNAME = "WebService"
ONEC_PASSWORD = "пароль_от_1с"

; Количество попыток при ошибке
ONEC_RETRY_ATTEMPTS = 3
```

**3.3. Установить права доступа**

Если есть SSH доступ, выполните:

```bash
sudo chown -R www-data:www-data /var/www/api.rosek.ru
sudo chmod 755 /var/www/api.rosek.ru
sudo chmod 777 /var/www/api.rosek.ru/orders
```

Если нет SSH - попросите хостинг это сделать.

---

## ШАГ 4: ВЫ → Обновить сайт (ПОСЛЕ ШАГА 3)

**Срок: 5 минут**

### Изменить URL API в коде сайта:

Откройте файл `js/cart.js` и найдите строку:

```javascript
// Было (локально):
const response = await fetch('api/orders.php', {
```

Замените на:

```javascript
// Стало (на VDS):
const response = await fetch('https://api.rosek.ru/api/orders.php', {
```

Сохраните и загрузите на GitHub.

---

## ШАГ 5: ВЫ → Проверка работы (ПОСЛЕ ВСЕХ ШАГОВ)

**Срок: 10 минут**

### 5.1. Проверить API сервер

Откройте в браузере:

```
https://api.rosek.ru/api/health.php
```

Должны увидеть:

```json
{
  "status": "ok",
  "timestamp": "2026-03-13 12:00:00"
}
```

### 5.2. Создать тестовый заказ

1. Откройте ваш сайт
2. Добавьте любой товар в корзину
3. Заполните форму заказа
4. Нажмите "Оформить заказ"

### 5.3. Проверить, что заказ сохранился

Через FTP зайдите в папку `/var/www/api.rosek.ru/orders/`

Должен появиться файл типа: `order-2026-03-13_14-30-25.json`

### 5.4. Проверить в 1С (вместе с программистом)

Попросите программиста проверить:
- Заказ появился в таблице "ВнешниеЗаказы"
- Создался документ "Заказ покупателя"
- Статус обновился

---

## ШАГ 6: Программист 1С → Финальная настройка

**Срок: 1 день**

### Что должен сделать программист:

**6.1. Настроить подключение к вашему серверу**

В настройках HTTP-сервиса 1С указать:
- URL: `https://api.rosek.ru/api/orders.php`
- Метод: GET (для получения списка заказов)

**6.2. Протестировать получение заказов**

Запустить обработку вручную и проверить:
- 1С видит новые заказы
- Данные корректно парсятся
- Документы создаются правильно

**6.3. Включить автоматическую обработку**

Активировать регламентное задание (каждые 5-10 минут)

---

## Итоговая схема работы

```
┌─────────────┐
│   КЛИЕНТ    │ Оформляет заказ на сайте
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────┐
│  САЙТ (rosek.ru)                    │
│  - Форма корзины                    │
│  - js/cart.js отправляет данные     │
└──────┬──────────────────────────────┘
       │ HTTPS POST
       ▼
┌─────────────────────────────────────┐
│  API СЕРВЕР (api.rosek.ru)          │
│  - api/orders.php                   │
│  - Сохраняет в orders/*.json        │
└──────┬──────────────────────────────┘
       │
       │ Каждые 5 минут
       ▼
┌─────────────────────────────────────┐
│  1С                                 │
│  - HTTP-сервис запрашивает заказы   │
│  - Скачивает JSON файлы             │
│  - Создает документы                │
│  - Отправляет подтверждение         │
└─────────────────────────────────────┘
```

---

## Кто за что отвечает - КРАТКО

| Кто | Что делает | Когда |
|-----|-----------|-------|
| **ХОСТИНГ** | Предоставляет VDS, настраивает поддомен и SSL | Шаг 1 (1-2 дня) |
| **ВЫ** | Заказываете VDS, загружаете файлы, настраиваете config.ini | Шаги 1, 3, 4 (1 день) |
| **ПРОГРАММИСТ 1С** | Создает HTTP-сервис, таблицы, обработку | Шаги 2, 6 (4-6 дней) |
| **ВСЕ ВМЕСТЕ** | Тестируете работу | Шаг 5 (1 день) |

---

## Общий срок: 7-10 дней

- Дни 1-2: Хостинг настраивает VDS
- Дни 1-5: Программист 1С делает свою часть
- День 6: Вы загружаете файлы и настраиваете
- День 7: Обновляете сайт
- Дни 8-9: Тестирование
- День 10: Запуск в работу

---

## Что делать ПРЯМО СЕЙЧАС

1. ✉️ Написать хостингу (текст выше в Шаге 1)
2. ✉️ Отправить программисту файл `1С_ИНТЕГРАЦИЯ.md` (текст выше в Шаге 2)
3. ⏳ Ждать ответа от обоих
4. 📋 Когда получите доступы - переходить к Шагу 3

---


