# Инструкция для программиста 1С

## 🔄 Обновление цен товаров

### Куда отправлять

**Для тестирования (локально):**
```
POST http://localhost:3000/api/products/update-prices
```

**Для продакшена:**
```
POST https://rosek.tech/api/products/update-prices
```

### Формат данных

```json
{
  "prices": [
    {"article": "AVR-25A-2V", "price": 15000},
    {"article": "AVR-32A-2V", "price": 18000},
    {"article": "NKU-100A", "price": 45000}
  ]
}
```

**Важно:**
- `article` - артикул товара (должен точно совпадать!)
- `price` - цена в рублях (целое число, без копеек)

### Ответ от сервера

**Успешно:**
```json
{
  "success": true,
  "message": "Цены обновлены",
  "updated": 3,
  "notFound": []
}
```

**Если артикулы не найдены:**
```json
{
  "success": true,
  "message": "Цены обновлены",
  "updated": 2,
  "notFound": ["UNKNOWN-ARTICLE"]
}
```

### Тест через curl

```bash
curl -X POST http://localhost:3000/api/products/update-prices \
  -H "Content-Type: application/json" \
  -d '{"prices":[{"article":"AVR-25A-2V","price":15000}]}'
```

---

## 📥 Получение заказов

### Куда приходят заказы

```
POST http://localhost:3000/api/orders
```

Заказы отправляются **автоматически** после успешной оплаты через СБП.

### Формат заказа

**Физическое лицо:**
```json
{
  "orderDate": "2024-02-07T23:59:52.000Z",
  "paymentId": "pm_123456789",
  "transactionId": "A50581324524670W0000040011450701",
  "paidAt": "2024-02-08T00:05:30.000Z",
  "clientType": "individual",
  "clientInfo": {
    "fullName": "Иванов Иван Иванович",
    "phone": "+7 (999) 123-45-67",
    "email": "ivan@example.com",
    "address": "г. Москва, ул. Ленина, д. 1",
    "comment": "Доставка после 18:00"
  },
  "items": [
    {
      "article": "AVR-25A-2V",
      "productTitle": "АВР на 25А с 2 вводами",
      "quantity": 2,
      "unitPrice": 15000,
      "totalPrice": 30000
    }
  ],
  "totalItems": 2,
  "totalPrice": 30000
}
```

**Юридическое лицо:**
```json
{
  "orderDate": "2024-02-07T23:59:52.000Z",
  "paymentId": "pm_123456789",
  "transactionId": "tx_123",
  "paidAt": "2024-02-08T00:05:30.000Z",
  "clientType": "legal",
  "clientInfo": {
    "companyName": "ООО Электромонтаж",
    "inn": "7701234567",
    "kpp": "770101001",
    "legalAddress": "г. Москва, ул. Промышленная, д. 5",
    "contactPerson": "Петров Петр Петрович",
    "phone": "+7 (999) 987-65-43",
    "email": "petrov@electro.ru",
    "comment": "Требуется счет"
  },
  "items": [...],
  "totalItems": 5,
  "totalPrice": 125000
}
```

### Основные поля

- `paymentId` - ID платежа в системе СБП
- `transactionId` - ID транзакции от банка
- `clientType` - Тип клиента: `"individual"` (физ. лицо) или `"legal"` (юр. лицо)
- `items[]` - Массив товаров
  - `article` - Артикул
  - `quantity` - Количество
  - `unitPrice` - Цена за единицу
  - `totalPrice` - Сумма (unitPrice × quantity)
- `totalPrice` - Общая сумма заказа

### Ответ на заказ

**Успешно:**
```json
{
  "success": true,
  "message": "Заказ успешно оформлен",
  "orderId": "order-2024-02-07T23-59-52"
}
```

**Ошибка:**
```json
{
  "success": false,
  "message": "Ошибка при оформлении заказа",
  "error": "Описание ошибки"
}
```

---

## 💻 Пример кода для 1С

### Обновление цен

```1c
Процедура ОбновитьЦеныНаСайте()
    
    АдресСервера = "http://localhost:3000"; // Для теста
    // АдресСервера = "https://rosek.tech"; // Для продакшена
    
    // Формируем массив цен
    МассивЦен = Новый Массив;
    
    // Получаем товары из 1С
    Запрос = Новый Запрос;
    Запрос.Текст = "
    |ВЫБРАТЬ
    |    Номенклатура.Артикул КАК article,
    |    Номенклатура.Цена КАК price
    |ИЗ
    |    Справочник.Номенклатура КАК Номенклатура
    |ГДЕ
    |    Номенклатура.ВыгружатьНаСайт = ИСТИНА
    |";
    
    Результат = Запрос.Выполнить();
    Выборка = Результат.Выбрать();
    
    Пока Выборка.Следующий() Цикл
        Цена = Новый Структура;
        Цена.Вставить("article", Выборка.article);
        Цена.Вставить("price", Выборка.price);
        МассивЦен.Добавить(Цена);
    КонецЦикла;
    
    // Формируем JSON
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    
    Данные = Новый Структура;
    Данные.Вставить("prices", МассивЦен);
    
    ЗаписатьJSON(ЗаписьJSON, Данные);
    ТелоЗапроса = ЗаписьJSON.Закрыть();
    
    // Отправляем запрос
    Соединение = Новый HTTPСоединение(АдресСервера, 3000);
    Запрос = Новый HTTPЗапрос("/api/products/update-prices");
    Запрос.Заголовки.Вставить("Content-Type", "application/json");
    Запрос.УстановитьТелоИзСтроки(ТелоЗапроса);
    
    Попытка
        Ответ = Соединение.ОтправитьДляОбработки(Запрос);
        
        Если Ответ.КодСостояния = 200 Тогда
            ЧтениеJSON = Новый ЧтениеJSON;
            ЧтениеJSON.УстановитьСтроку(Ответ.ПолучитьТелоКакСтроку());
            Результат = ПрочитатьJSON(ЧтениеJSON);
            ЧтениеJSON.Закрыть();
            
            Сообщить("Обновлено цен: " + Результат.updated);
        Иначе
            Сообщить("Ошибка: " + Ответ.КодСостояния);
        КонецЕсли;
    Исключение
        Сообщить("Ошибка: " + ОписаниеОшибки());
    КонецПопытки;
    
КонецПроцедуры
```

---

## 📞 Контакты

При возникновении вопросов:
- Email: enogovicina167@gmail.com
- Телефон: 8 (800) 55-11-052
