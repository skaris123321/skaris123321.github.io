# Инструкция по запуску сайта РОСЭК

## Как работает система

### Схема работы заказов:

```
Клиент оформляет заказ → api/orders.php → отправляет в 1С → создается документ "Заказ покупателя"
                              ↓
                    сохраняет в orders/*.json (резервная копия)
```

**Пошагово:**
1. Клиент заполняет форму заказа на сайте (товары, контакты)
2. Нажимает "Оплатить"
3. PHP скрипт `api/orders.php`:
   - Сохраняет заказ в файл `orders/order-2026-03-09_15-30-45.json` (резервная копия)
   - Отправляет заказ в 1С через HTTP API
4. В 1С автоматически создается документ "Заказ покупателя" с:
   - Контрагентом (создается если нет)
   - Товарами из заказа
   - Контактными данными
5. Клиент видит QR-код для оплаты

**Если 1С недоступна:**
- Заказ все равно сохранится в файл
- PHP попробует отправить 3 раза с задержкой
- Вы увидите заказ в папке `orders/` и сможете обработать вручную

---

## Для вас (владелец сайта)

### Шаг 1: Добавить QR-код для оплаты

Положите изображение QR-кода вашего банка в папку `images/` с именем `qr-payment.png`

### Шаг 2: Настроить config.ini

Откройте файл `config.ini` в корне сайта и заполните:

```ini
; Придумайте любой сложный токен (буквы, цифры, дефисы)
API_TOKEN = "rosek-2026-my-secret-token-xyz789"

; Эти данные даст работник 1С после настройки
ONEC_API_URL = "http://192.168.1.50:8080/accounting/hs/orders/create"
ONEC_USERNAME = "WebService"
ONEC_PASSWORD = "1cPassword123"
ONEC_RETRY_ATTEMPTS = 3
```

**Где взять данные 1С:**
- Работник 1С настроит HTTP-сервис (см. ниже)
- Он даст вам URL, логин и пароль
- Вставьте их в config.ini

### Шаг 3: Загрузить на хостинг

1. Загрузите все файлы на хостинг через FTP/SFTP
2. Убедитесь что PHP 7.4+ установлен (обычно уже есть)
3. Готово! Сайт работает

### Шаг 4: Проверить работу

Откройте в браузере:
- `https://ваш-домен.ru` - должен открыться сайт
- `https://ваш-домен.ru/api/health.php` - должно показать `{"status":"ok"}`

---

## Для работника 1С

### Что приходит от сайта в 1С

Когда клиент оформляет заказ, в 1С приходит JSON с данными:

**Пример для физического лица:**
```json
{
  "orderDate": "2026-03-09T15:30:45",
  "clientType": "individual",
  "clientInfo": {
    "fullName": "Иванов Иван Иванович",
    "phone": "+7 (999) 123-45-67",
    "email": "ivanov@mail.ru",
    "address": "г. Москва, ул. Ленина, д. 10"
  },
  "items": [
    {
      "article": "АВР-25А-2В-CHINT",
      "productTitle": "Моноблочный АВР 25А 2 ввода",
      "manufacturerBrand": "CHINT",
      "quantity": 2,
      "price": 45000,
      "totalPrice": 90000
    }
  ],
  "totalItems": 2,
  "totalPrice": 90000,
  "comment": "Срочная доставка"
}
```

**Пример для юридического лица:**
```json
{
  "orderDate": "2026-03-09T15:30:45",
  "clientType": "legal",
  "clientInfo": {
    "companyName": "ООО «Электромонтаж»",
    "inn": "7701234567",
    "kpp": "770101001",
    "legalAddress": "г. Москва, ул. Промышленная, д. 5",
    "contactPerson": "Петров Петр Петрович",
    "phone": "+7 (999) 123-45-67",
    "email": "info@electro.ru"
  },
  "items": [
    {
      "article": "Я5000-0.6-РОСЭК",
      "productTitle": "Ящик управления прямой пуск 0.6 кВт",
      "manufacturerBrand": "IEK",
      "quantity": 5,
      "price": 15000,
      "totalPrice": 75000
    }
  ],
  "totalItems": 5,
  "totalPrice": 75000,
  "comment": ""
}
```

### Что делает код в 1С

1. **Находит или создает контрагента:**
   - Для юр.лица ищет по ИНН
   - Для физ.лица ищет по ФИО
   - Если не найден - создает нового

2. **Находит товары по артикулу:**
   - Ищет в справочнике Номенклатура по полю Артикул
   - Если не найден - добавляет в комментарий заказа

3. **Создает документ "Заказ покупателя":**
   - Заполняет контрагента
   - Добавляет товары с ценами и количеством
   - Сохраняет контакты (телефон, email)
   - Проводит документ

4. **Отправляет ответ на сайт:**
   - `{"success": true, "orderId": "...", "orderNumber": "..."}`

---

## Для работника 1С (настройка)

### Что нужно сделать в 1С

1. Открыть конфигуратор 1С
2. Создать HTTP-сервис с именем `orders`
3. Создать 2 метода:
   - `create` (POST) - для приема заказов
   - `ping` (GET) - для проверки

### Код для метода create (прием заказов)

```1c
Функция CreateOrderПОСТ(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.Заголовки.Вставить("Content-Type", "application/json");
    
    Попытка
        // Читаем JSON
        ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку(КодировкаТекста.UTF8);
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(ТелоЗапроса);
        ДанныеЗаказа = ПрочитатьJSON(ЧтениеJSON);
        ЧтениеJSON.Закрыть();
        
        // Создаем заказ
        НовыйЗаказ = Документы.ЗаказПокупателя.СоздатьДокумент();
        НовыйЗаказ.Дата = ТекущаяДата();
        
        // Находим/создаем контрагента
        Если ДанныеЗаказа.clientType = "legal" Тогда
            Контрагент = НайтиИлиСоздатьКонтрагента(
                ДанныеЗаказа.clientInfo.companyName,
                ДанныеЗаказа.clientInfo.inn,
                ДанныеЗаказа.clientInfo.kpp,
                Истина
            );
        Иначе
            Контрагент = НайтиИлиСоздатьКонтрагента(
                ДанныеЗаказа.clientInfo.fullName,
                "",
                "",
                Ложь
            );
        КонецЕсли;
        
        НовыйЗаказ.Контрагент = Контрагент;
        НовыйЗаказ.ТелефонКонтрагента = ДанныеЗаказа.clientInfo.phone;
        НовыйЗаказ.EmailКонтрагента = ДанныеЗаказа.clientInfo.email;
        
        // Добавляем товары
        Для Каждого Товар Из ДанныеЗаказа.items Цикл
            НоваяСтрока = НовыйЗаказ.Товары.Добавить();
            Номенклатура = НайтиНоменклатуруПоАртикулу(Товар.article);
            
            Если Номенклатура <> Неопределено Тогда
                НоваяСтрока.Номенклатура = Номенклатура;
                НоваяСтрока.Количество = Товар.quantity;
                НоваяСтрока.Цена = Товар.price;
                НоваяСтрока.Сумма = Товар.totalPrice;
            КонецЕсли;
        КонецЦикла;
        
        // Сохраняем
        НовыйЗаказ.Записать(РежимЗаписиДокумента.Проведение);
        
        // Отправляем ответ
        РезультатJSON = Новый Структура;
        РезультатJSON.Вставить("success", Истина);
        РезультатJSON.Вставить("orderId", Строка(НовыйЗаказ.Ссылка.УникальныйИдентификатор()));
        РезультатJSON.Вставить("orderNumber", НовыйЗаказ.Номер);
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
        Ответ.УстановитьТелоИзСтроки(ЗаписьJSON.Закрыть());
        
    Исключение
        РезультатJSON = Новый Структура;
        РезультатJSON.Вставить("success", Ложь);
        РезультатJSON.Вставить("error", ОписаниеОшибки());
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
        
        Ответ.КодСостояния = 500;
        Ответ.УстановитьТелоИзСтроки(ЗаписьJSON.Закрыть());
    КонецПопытки;
    
    Возврат Ответ;
    
КонецФункции

Функция НайтиИлиСоздатьКонтрагента(Наименование, ИНН, КПП, ЭтоЮрЛицо)
    Запрос = Новый Запрос;
    
    Если ЭтоЮрЛицо И НЕ ПустаяСтрока(ИНН) Тогда
        Запрос.Текст = "ВЫБРАТЬ ПЕРВЫЕ 1 Контрагенты.Ссылка ИЗ Справочник.Контрагенты ГДЕ Контрагенты.ИНН = &ИНН";
        Запрос.УстановитьПараметр("ИНН", ИНН);
    Иначе
        Запрос.Текст = "ВЫБРАТЬ ПЕРВЫЕ 1 Контрагенты.Ссылка ИЗ Справочник.Контрагенты ГДЕ Контрагенты.Наименование = &Наименование";
        Запрос.УстановитьПараметр("Наименование", Наименование);
    КонецЕсли;
    
    Результат = Запрос.Выполнить();
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    КонецЕсли;
    
    НовыйКонтрагент = Справочники.Контрагенты.СоздатьЭлемент();
    НовыйКонтрагент.Наименование = Наименование;
    Если ЭтоЮрЛицо Тогда
        НовыйКонтрагент.ИНН = ИНН;
        НовыйКонтрагент.КПП = КПП;
    КонецЕсли;
    НовыйКонтрагент.Записать();
    
    Возврат НовыйКонтрагент.Ссылка;
КонецФункции

Функция НайтиНоменклатуруПоАртикулу(Артикул)
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ ПЕРВЫЕ 1 Номенклатура.Ссылка ИЗ Справочник.Номенклатура ГДЕ Номенклатура.Артикул = &Артикул";
    Запрос.УстановитьПараметр("Артикул", Артикул);
    
    Результат = Запрос.Выполнить();
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    КонецЕсли;
    
    Возврат Неопределено;
КонецФункции
```

### Код для метода ping (проверка)

```1c
Функция pingGET(Запрос)
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.Заголовки.Вставить("Content-Type", "application/json");
    
    РезультатJSON = Новый Структура;
    РезультатJSON.Вставить("status", "ok");
    
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
    Ответ.УстановитьТелоИзСтроки(ЗаписьJSON.Закрыть());
    
    Возврат Ответ;
КонецФункции
```

### Настройка пользователя

1. Создайте пользователя `WebService` с паролем
2. Дайте права на создание документов "Заказ покупателя"
3. Включите базовую аутентификацию в HTTP-сервисе

### Узнать URL сервиса

URL будет вида: `http://сервер:порт/база/hs/orders/create`

Примеры:
- `http://localhost/accounting/hs/orders/create`
- `http://192.168.1.100:8080/accounting/hs/orders/create`

### Передать данные владельцу сайта

Сообщите:
- URL: `http://...`
- Логин: `WebService`
- Пароль: `ваш-пароль`

Владелец сайта вставит эти данные в `config.ini`

---

## Автоматическое обновление цен (опционально)

### Как это работает:

```
1С → отправляет новые цены → api/update-prices.php → обновляет data/*.json → клиенты видят новые цены
```

**Пример:**
1. В 1С цена товара изменилась с 10,000₽ на 12,000₽
2. 1С отправляет: `{"prices": [{"article": "АВР-25А", "price": 12000}]}`
3. PHP скрипт находит товар по артикулу в файлах `data/products-*.json`
4. Обновляет `"base_price": 12000` в JSON файле
5. Клиент открывает сайт и видит новую цену 12,000₽

### Настройка в 1С

Если нужно обновлять цены на сайте из 1С, создайте процедуру:

```1c
Процедура ВыгрузитьЦеныНаСайт()
    МассивЦен = Новый Массив;
    
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ Номенклатура.Артикул, Номенклатура.Цена ИЗ Справочник.Номенклатура ГДЕ Номенклатура.ВыгружатьНаСайт = ИСТИНА";
    
    Результат = Запрос.Выполнить();
    Выборка = Результат.Выбрать();
    
    Пока Выборка.Следующий() Цикл
        Элемент = Новый Структура;
        Элемент.Вставить("article", Выборка.Артикул);
        Элемент.Вставить("price", Выборка.Цена);
        МассивЦен.Добавить(Элемент);
    КонецЦикла;
    
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    Данные = Новый Структура;
    Данные.Вставить("prices", МассивЦен);
    ЗаписатьJSON(ЗаписьJSON, Данные);
    JSON = ЗаписьJSON.Закрыть();
    
    // Замените на ваш домен и токен из config.ini
    Соединение = Новый HTTPСоединение("ваш-домен.ru", 443, , , , 30, Новый ЗащищенноеСоединениеOpenSSL);
    Запрос = Новый HTTPЗапрос("/api/update-prices.php");
    Запрос.Заголовки.Вставить("Content-Type", "application/json");
    Запрос.Заголовки.Вставить("Authorization", "Bearer ваш-токен-из-config-ini");
    Запрос.УстановитьТелоИзСтроки(JSON, КодировкаТекста.UTF8);
    
    Попытка
        Ответ = Соединение.ОтправитьДляОбработки(Запрос);
        Если Ответ.КодСостояния = 200 Тогда
            Сообщить("Цены обновлены!");
        КонецЕсли;
    Исключение
        Сообщить("Ошибка: " + ОписаниеОшибки());
    КонецПопытки;
КонецПроцедуры
```

Создайте регламентное задание для автоматического запуска (например, каждый час).

### Что происходит на сайте:

1. **PHP скрипт получает данные** от 1С
2. **Открывает JSON файлы** из папки `data/`:
   - `products-avr.json`
   - `products-control-cabinets.json`
   - `products-motor-control-boxes.json`
   - `products-reactive-power.json`
3. **Находит товары по артикулу** и обновляет поле `base_price`
4. **Сохраняет файлы** обратно
5. **Клиенты сразу видят** новые цены при открытии страниц

### Пример обновления:

**Было в data/products-avr.json:**
```json
{
  "article": "АВР-25А-2В-CHINT",
  "base_price": 45000
}
```

**1С отправил:**
```json
{
  "prices": [
    {"article": "АВР-25А-2В-CHINT", "price": 48000}
  ]
}
```

**Стало в data/products-avr.json:**
```json
{
  "article": "АВР-25А-2В-CHINT",
  "base_price": 48000
}
```

**Клиент видит:** Цена обновилась с 45,000₽ на 48,000₽

### Безопасность:

- API защищен токеном из `config.ini`
- Только 1С с правильным токеном может обновлять цены
- Если токен неверный - ошибка 401 Unauthorized

---

## Проверка работы

### Проверка 1С:
```bash
curl -u WebService:пароль http://адрес-1с/база/hs/orders/ping
```

### Проверка сайта:
```bash
curl https://ваш-домен.ru/api/health.php
```

---

## Что делать если что-то не работает

1. **Заказы не приходят в 1С**
   - Проверьте URL в config.ini
   - Проверьте логин/пароль
   - Посмотрите файлы в папке `orders/` - там резервные копии

2. **Не обновляются цены**
   - Проверьте токен в config.ini
   - Проверьте что токен совпадает в 1С коде

3. **Ошибка 500**
   - Проверьте что PHP 7.4+ установлен
   - Проверьте права на папки `orders/` и `data/`

---

## Контакты

Email: zakaz@rosek.tech
Телефон: 8 (800) 55-11-052
