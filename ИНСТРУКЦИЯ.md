# Инструкция по запуску сайта РОСЭК

⚠️ **ВАЖНОЕ ТРЕБОВАНИЕ:**

**Для автоматической работы интернет-магазина нужна СЕРВЕРНАЯ версия 1С!**

Если у вас:
- 1С запускается с ярлыка на рабочем столе
- Путь к базе `\\server\folder\` или `C:\bases\`
- В свойствах ярлыка `/F"путь"`

**ЭТО ФАЙЛОВАЯ ВЕРСИЯ** - она НЕ РАБОТАЕТ с автоматической интеграцией!

**Обратитесь к IT-специалисту** для перевода на серверную версию.

---

Эта инструкция описывает пошаговый процесс запуска интернет-магазина с автоматической интеграцией 1С.

## Порядок действий

1. **Владелец сайта** (ШАГ 1) - готовит сайт и передает данные работнику 1С
2. **Работник 1С** (ШАГ 2) - настраивает 1С и передает данные владельцу сайта
3. **Владелец сайта** (ШАГ 3 и 4) - завершает настройку и загружает на хостинг

---

## Важно понимать

### Два адреса

**1. Адрес сайта (домен)**
- Это то, что клиенты набирают в браузере: `rosek.ru`
- Находится на хостинге

**2. Адрес 1С**
- Это адрес сервера где установлена 1С
- Примеры: `http://85.123.45.67:8080/accounting/hs/orders/create`

### Как это работает

```
Клиент → оформляет заказ на rosek.ru
           ↓
Сайт → отправляет заказ в 1С
           ↓
1С → создает документ "Заказ покупателя"
```

### Где взять адрес 1С

⚠️ **Для автоматической интеграции нужна СЕРВЕРНАЯ версия 1С!**

#### Проверьте тип вашей 1С:

**Если 1С открывается через браузер:**
```
http://1c.company.ru
https://server.local/1c
```
**ЭТО СЕРВЕРНАЯ ВЕРСИЯ** - она РАБОТАЕТ с автоматической интеграцией!

**Спросите у администратора 1С:**
- "Какой адрес сервера 1С для HTTP-сервисов?"
- "На каком порту работают веб-сервисы 1С?"

**Адрес будет выглядеть так:**
```
http://192.168.1.50:8080/accounting/hs/orders/create
http://1c.company.ru/base/hs/orders/create
```

#### Если у вас файловая версия 1С:

**Обратитесь к системному администратору или программисту 1С:**

> "Нужно настроить автоматическую интеграцию 1С с интернет-сайтом. Сейчас у нас файловая версия 1С. Для автоматической работы нужно:
> 
> 1. **Установить сервер 1С:Предприятие** на один из компьютеров
> 2. **Перенести базу данных** с файловой на серверную версию  
> 3. **Настроить HTTP-сервисы** для автоматического приема заказов
> 4. **Открыть порты** в файерволе для доступа из интернета
> 
> Это стандартная задача. После настройки дайте мне адрес вида:
> `http://server-ip:8080/database/hs/orders/create`"

#### Для 1С:Fresh или 1С:Облако:

**Обратитесь в поддержку провайдера:**
- Спросите: "Какой URL для HTTP-сервиса автоматической интеграции?"
- Обычно: `https://ваша-база.1cfresh.com/hs/orders/create`

**Важно:** Без серверной версии 1С автоматическая интеграция работать НЕ БУДЕТ!

---

# ШАГ 1: Подготовка сайта (владелец сайта)

## 1.1. Добавить QR-код для оплаты

1. Получите QR-код от вашего банка для приема платежей
2. Сохраните изображение как `qr-payment.png`
3. Положите файл в папку `images/`

Путь должен быть: `images/qr-payment.png`

## 1.2. Придумать токен для API

Придумайте сложный токен (буквы, цифры, дефисы).

Например: `rosek-2026-secret-xyz789-abc123`

**Зачем это нужно:**
Этот токен защищает API обновления цен на сайте. Только тот, кто знает токен (работник 1С), сможет обновлять цены. Без токена никто посторонний не сможет изменить цены на вашем сайте.

Запишите токен - он понадобится работнику 1С!

## 1.3. Узнать домен сайта

Узнайте у хостинг-провайдера или определите сами:
- Основной домен: `rosek.ru`
- Или поддомен: `shop.rosek.ru`

Запишите домен!

## 1.4. Передать данные работнику 1С

Передайте работнику 1С два параметра:
1. **Домен сайта**: например `rosek.ru` (нужен для обновления цен из 1С на сайт)
2. **Токен API**: например `rosek-2026-secret-xyz789-abc123` (для защиты API обновления цен)

Скажите: "Настрой пожалуйста прием заказов и обновление цен по инструкции ШАГ 2"

---

# ШАГ 2: Настройка 1С (работник 1С)

Вы получили от владельца сайта:
- **Домен сайта**: например `rosek.ru`
- **Токен API**: например `rosek-2026-secret-xyz789-abc123`

Теперь настройте 1С для приема заказов и обновления цен.

## 2.1. Создать HTTP-сервис

1. Откройте 1С в режиме Конфигуратора
2. В дереве конфигурации найдите раздел "HTTP-сервисы"
3. Правой кнопкой мыши → Добавить
4. Заполните:
   - Имя: `orders`
   - Корневой URL: `orders`
   - Описание: `Прием заказов с сайта`
5. Поставьте галочку "Базовая аутентификация"

## 2.2. Создать URL шаблон для приема заказов

1. В HTTP-сервисе `orders` создайте URL шаблон
2. Правой кнопкой → Добавить
3. Заполните:
   - Имя: `create`
   - Шаблон: `create`
   - HTTP метод: `POST`
4. Создайте метод с именем `CreateOrder`

## 2.3. Вставить код метода CreateOrder

Скопируйте и вставьте этот код в метод `CreateOrder`:

```1c
Функция CreateOrderПОСТ(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.Заголовки.Вставить("Content-Type", "application/json");
    
    Попытка
        // Читаем JSON
        ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку(КодировкаТекста.UTF8);
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(ТелоЗапроса);
        ДанныеЗаказа = ПрочитатьJSON(ЧтениеJSON);
        ЧтениеJSON.Закрыть();
        
        // Создаем заказ
        НовыйЗаказ = Документы.ЗаказПокупателя.СоздатьДокумент();
        НовыйЗаказ.Дата = ТекущаяДата();
        
        // Находим/создаем контрагента
        Если ДанныеЗаказа.clientType = "legal" Тогда
            Контрагент = НайтиИлиСоздатьКонтрагента(
                ДанныеЗаказа.clientInfo.companyName,
                ДанныеЗаказа.clientInfo.inn,
                ДанныеЗаказа.clientInfo.kpp,
                Истина
            );
        Иначе
            Контрагент = НайтиИлиСоздатьКонтрагента(
                ДанныеЗаказа.clientInfo.fullName,
                "",
                "",
                Ложь
            );
        КонецЕсли;
        
        НовыйЗаказ.Контрагент = Контрагент;
        НовыйЗаказ.ТелефонКонтрагента = ДанныеЗаказа.clientInfo.phone;
        НовыйЗаказ.EmailКонтрагента = ДанныеЗаказа.clientInfo.email;
        
        // Добавляем товары
        Для Каждого Товар Из ДанныеЗаказа.items Цикл
            НоваяСтрока = НовыйЗаказ.Товары.Добавить();
            Номенклатура = НайтиНоменклатуруПоАртикулу(Товар.article);
            
            Если Номенклатура <> Неопределено Тогда
                НоваяСтрока.Номенклатура = Номенклатура;
                НоваяСтрока.Количество = Товар.quantity;
                НоваяСтрока.Цена = Товар.price;
                НоваяСтрока.Сумма = Товар.totalPrice;
            КонецЕсли;
        КонецЦикла;
        
        // Сохраняем
        НовыйЗаказ.Записать(РежимЗаписиДокумента.Проведение);
        
        // Отправляем ответ
        РезультатJSON = Новый Структура;
        РезультатJSON.Вставить("success", Истина);
        РезультатJSON.Вставить("orderId", Строка(НовыйЗаказ.Ссылка.УникальныйИдентификатор()));
        РезультатJSON.Вставить("orderNumber", НовыйЗаказ.Номер);
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
        Ответ.УстановитьТелоИзСтроки(ЗаписьJSON.Закрыть());
        
    Исключение
        РезультатJSON = Новый Структура;
        РезультатJSON.Вставить("success", Ложь);
        РезультатJSON.Вставить("error", ОписаниеОшибки());
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
        
        Ответ.КодСостояния = 500;
        Ответ.УстановитьТелоИзСтроки(ЗаписьJSON.Закрыть());
    КонецПопытки;
    
    Возврат Ответ;
    
КонецФункции

Функция НайтиИлиСоздатьКонтрагента(Наименование, ИНН, КПП, ЭтоЮрЛицо)
    Запрос = Новый Запрос;
    
    Если ЭтоЮрЛицо И НЕ ПустаяСтрока(ИНН) Тогда
        Запрос.Текст = "ВЫБРАТЬ ПЕРВЫЕ 1 Контрагенты.Ссылка ИЗ Справочник.Контрагенты ГДЕ Контрагенты.ИНН = &ИНН";
        Запрос.УстановитьПараметр("ИНН", ИНН);
    Иначе
        Запрос.Текст = "ВЫБРАТЬ ПЕРВЫЕ 1 Контрагенты.Ссылка ИЗ Справочник.Контрагенты ГДЕ Контрагенты.Наименование = &Наименование";
        Запрос.УстановитьПараметр("Наименование", Наименование);
    КонецЕсли;
    
    Результат = Запрос.Выполнить();
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    КонецЕсли;
    
    НовыйКонтрагент = Справочники.Контрагенты.СоздатьЭлемент();
    НовыйКонтрагент.Наименование = Наименование;
    Если ЭтоЮрЛицо Тогда
        НовыйКонтрагент.ИНН = ИНН;
        НовыйКонтрагент.КПП = КПП;
    КонецЕсли;
    НовыйКонтрагент.Записать();
    
    Возврат НовыйКонтрагент.Ссылка;
КонецФункции

Функция НайтиНоменклатуруПоАртикулу(Артикул)
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ ПЕРВЫЕ 1 Номенклатура.Ссылка ИЗ Справочник.Номенклатура ГДЕ Номенклатура.Артикул = &Артикул";
    Запрос.УстановитьПараметр("Артикул", Артикул);
    
    Результат = Запрос.Выполнить();
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    КонецЕсли;
    
    Возврат Неопределено;
КонецФункции
```

## 2.4. Создать URL шаблон для проверки

1. В HTTP-сервисе `orders` создайте еще один URL шаблон
2. Правой кнопкой → Добавить
3. Заполните:
   - Имя: `ping`
   - Шаблон: `ping`
   - HTTP метод: `GET`
4. Создайте метод с именем `ping`

## 2.5. Вставить код метода ping

```1c
Функция pingGET(Запрос)
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.Заголовки.Вставить("Content-Type", "application/json");
    
    РезультатJSON = Новый Структура;
    РезультатJSON.Вставить("status", "ok");
    
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
    Ответ.УстановитьТелоИзСтроки(ЗаписьJSON.Закрыть());
    
    Возврат Ответ;
КонецФункции
```

## 2.6. Создать пользователя для сайта

1. Откройте 1С:Предприятие (не конфигуратор)
2. Перейдите в "Администрирование" → "Пользователи"
3. Создайте нового пользователя:
   - Имя: `WebService`
   - Придумайте надежный пароль (запишите его!)
   - Права: дайте права на создание документов "Заказ покупателя"

## 2.7. Настроить автоматическое обновление цен

Теперь настроим автоматическую выгрузку цен на сайт. Используйте домен и токен которые вам передал владелец сайта.

### 2.7.1. Создать процедуру

1. Откройте конфигуратор 1С
2. Создайте общий модуль или обработку
3. Добавьте процедуру:

```1c
Процедура ВыгрузитьЦеныНаСайт()
    МассивЦен = Новый Массив;
    
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ Номенклатура.Артикул, Номенклатура.Цена ИЗ Справочник.Номенклатура ГДЕ Номенклатура.ВыгружатьНаСайт = ИСТИНА";
    
    Результат = Запрос.Выполнить();
    Выборка = Результат.Выбрать();
    
    Пока Выборка.Следующий() Цикл
        Элемент = Новый Структура;
        Элемент.Вставить("article", Выборка.Артикул);
        Элемент.Вставить("price", Выборка.Цена);
        МассивЦен.Добавить(Элемент);
    КонецЦикла;
    
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    Данные = Новый Структура;
    Данные.Вставить("prices", МассивЦен);
    ЗаписатьJSON(ЗаписьJSON, Данные);
    JSON = ЗаписьJSON.Закрыть();
    
    // ВАЖНО: Замените на данные от владельца сайта:
    // Домен: например rosek.ru
    // Токен: например rosek-2026-secret-xyz789-abc123
    Соединение = Новый HTTPСоединение("ваш-домен.ru", 443, , , , 30, Новый ЗащищенноеСоединениеOpenSSL);
    Запрос = Новый HTTPЗапрос("/api/update-prices.php");
    Запрос.Заголовки.Вставить("Content-Type", "application/json");
    Запрос.Заголовки.Вставить("Authorization", "Bearer ваш-токен-от-владельца");
    Запрос.УстановитьТелоИзСтроки(JSON, КодировкаТекста.UTF8);
    
    Попытка
        Ответ = Соединение.ОтправитьДляОбработки(Запрос);
        Если Ответ.КодСостояния = 200 Тогда
            Сообщить("Цены обновлены на сайте!");
        Иначе
            Сообщить("Ошибка: " + Ответ.КодСостояния);
        КонецЕсли;
    Исключение
        Сообщить("Ошибка: " + ОписаниеОшибки());
    КонецПопытки;
КонецПроцедуры
```

### 2.7.2. Создать регламентное задание

1. В 1С:Предприятие перейдите в "Администрирование" → "Регламентные задания"
2. Создайте новое задание:
   - Имя: "Выгрузка цен на сайт"
   - Метод: `ВыгрузитьЦеныНаСайт`
   - Расписание: каждый час
3. Сохраните

## 2.8. Опубликовать HTTP-сервис

1. Сохраните конфигурацию (Ctrl+S)
2. Обновите конфигурацию базы данных (Конфигурация → Обновить конфигурацию базы данных)
3. Перезапустите 1С:Предприятие

## 2.9. Узнать URL сервиса

URL вашего HTTP-сервиса будет иметь вид:
```
http://[адрес-сервера]:[порт]/[имя-базы]/hs/orders/create
```

Примеры:
- `http://192.168.1.100:8080/accounting/hs/orders/create`
- `http://1c.company.ru/base/hs/orders/create`

## 2.10. Настроить доступ из интернета

⚠️ **Этот шаг должен выполнять системный администратор!**

**Скажите администратору:**

> "Нужно настроить доступ к 1С из интернета для работы сайта:
> 
> 1. **Открыть порт 8080** в файерволе сервера
> 2. **Настроить проброс портов** на роутере (если нужен внешний доступ)
> 3. **Узнать внешний IP-адрес** компании
> 4. **Проверить доступность** HTTP-сервиса из интернета
> 
> После настройки дайте мне внешний адрес вида:
> `http://внешний-IP:8080/база/hs/orders/create`"

**Для безопасности рекомендуется:**
- Использовать HTTPS вместо HTTP
- Ограничить доступ только с IP-адреса хостинга сайта
- Использовать нестандартный порт (например, 8443)

## 2.11. Проверить работу

Откройте командную строку и выполните:
```bash
curl -u WebService:ваш-пароль http://адрес-сервера/база/hs/orders/ping
```

Должен вернуться ответ: `{"status":"ok"}`

**Если не работает:**
- Проверьте что сервер 1С запущен
- Проверьте что HTTP-сервис опубликован
- Проверьте файервол и сетевые настройки

## 2.12. Передать данные владельцу сайта

Передайте владельцу сайта три параметра:
1. **URL 1С**: `http://адрес-сервера/база/hs/orders/create`
2. **Логин**: `WebService`
3. **Пароль**: ваш-пароль

Скажите: "1С настроена, вот данные для config.ini"

---

# ШАГ 3: Завершение настройки (владелец сайта)

Вы получили от работника 1С:
- **URL 1С**: например `http://192.168.1.50:8080/accounting/hs/orders/create`
- **Логин**: `WebService`
- **Пароль**: например `MyPassword123`

## 3.1. Настроить config.ini

1. Откройте файл `config.ini` в корне сайта
2. Замените значения на свои:

```ini
; Токен который вы придумали в шаге 1.2
API_TOKEN = "rosek-2026-secret-xyz789-abc123"

; Данные от работника 1С для автоматической интеграции
ONEC_API_URL = "http://192.168.1.50:8080/accounting/hs/orders/create"
ONEC_USERNAME = "WebService"
ONEC_PASSWORD = "MyPassword123"
ONEC_RETRY_ATTEMPTS = 3
```

## 3.2. Проверить файлы

Убедитесь что у вас есть все файлы:
- `config.ini` - настроен
- `images/qr-payment.png` - добавлен
- `api/` - папка с PHP скриптами
- `data/` - папка с товарами
- `orders/` - папка для заказов (пустая)
- `.htaccess` - настройки Apache

---

# ШАГ 4: Загрузка на хостинг (владелец сайта)

## 4.1. Подключиться к хостингу

Используйте FTP/SFTP клиент (FileZilla, WinSCP) или файловый менеджер хостинга.

**Данные для подключения** (получите у хостинг-провайдера):
- Хост: ftp.ваш-домен.ru
- Логин: ваш-логин
- Пароль: ваш-пароль
- Порт: 21 (FTP) или 22 (SFTP)

## 4.2. Загрузить файлы

1. Подключитесь к хостингу
2. Перейдите в корневую папку сайта (обычно `public_html` или `www`)
3. Загрузите ВСЕ файлы проекта

**ВАЖНО:** Загружайте именно файлы, а не папку проекта целиком!

## 4.3. Проверить требования хостинга

Убедитесь что на хостинге установлено:
- PHP 7.4 или выше
- Модуль PHP cURL
- Apache с mod_rewrite (или Nginx)

## 4.4. Настроить права доступа

Установите права:

**Для папок:**
- `orders/` - права 755
- `data/` - права 755

**Для файлов:**
- `config.ini` - права 644
- Все остальные файлы - права 644

## 4.5. Проверить работу сайта

1. Откройте: `https://ваш-домен.ru`
   - Должна открыться главная страница

2. Откройте: `https://ваш-домен.ru/api/health.php`
   - Должно показать: `{"status":"ok","message":"Сервер работает"}`

3. Откройте: `https://ваш-домен.ru/category/catalog.html`
   - Должны отображаться товары

## 4.6. Проверить интеграцию с 1С

1. Оформите тестовый заказ на сайте
2. Проверьте что заказ появился в 1С
3. Проверьте что файл заказа создался в папке `orders/`

Если заказ не появился в 1С:
- Проверьте URL в `config.ini`
- Проверьте логин/пароль
- Посмотрите файл заказа в папке `orders/`

---

# Решение проблем

## У вас файловая версия 1С?

### Признаки файловой версии:
- 1С запускается с ярлыка на рабочем столе
- Путь к базе выглядит как `\\server\folder\` или `C:\bases\`
- В свойствах ярлыка написано `/F"путь-к-папке"`

### ❌ Файловая версия НЕ ПОДДЕРЖИВАЕТ автоматическую интеграцию!

### ✅ Что нужно сделать:

**Скажите вашему IT-специалисту:**

> "Для автоматической работы интернет-магазина нужно перевести 1С на серверную версию:
> 
> 1. Установить **сервер 1С:Предприятие** на компьютер или сервер
> 2. Создать **информационную базу** на сервере  
> 3. **Перенести данные** из файловой базы в серверную
> 4. Настроить **HTTP-сервисы** для автоматического приема заказов
> 5. Настроить **автоматическое обновление цен** на сайте
> 6. Открыть **порт 8080** в файерволе для доступа из интернета
> 
> После настройки интернет-магазин будет работать полностью автоматически!"

## Заказы не приходят в 1С автоматически

1. Проверьте `config.ini` - URL, логин, пароль правильные?
2. Проверьте папку `orders/` - есть ли там файлы заказов?
3. Проверьте доступность 1С:
   ```bash
   curl -u WebService:пароль http://адрес-1с/база/hs/orders/ping
   ```

## Не обновляются цены

1. Проверьте токен в `config.ini` и в коде 1С - они должны совпадать
2. Проверьте что в 1С у товаров заполнено поле "Артикул"
3. Проверьте что артикулы в 1С совпадают с артикулами в файлах `data/*.json`

## Ошибка 500 на сайте

1. Проверьте что PHP 7.4+ установлен
2. Проверьте права на папки `orders/` и `data/` (должно быть 755)
3. Проверьте логи PHP на хостинге

## Товары не отображаются

1. Проверьте что файлы из папки `data/` загружены на хостинг
2. Проверьте права на файлы (должно быть 644)
3. Откройте: `https://ваш-домен.ru/data/products-avr.json`
   - Должен открыться JSON с товарами

---

# Контакты

Email: zakaz@rosek.tech
Телефон: 8 (800) 55-11-052
