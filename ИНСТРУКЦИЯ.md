# Инструкция по запуску сайта РОСЭК

Эта инструкция описывает пошаговый процесс запуска интернет-магазина с интеграцией 1С.

---

## Важно понимать перед началом

### Что такое домен и где что находится

**У вас будет ДВА разных адреса:**

#### 1. Адрес САЙТА (домен или поддомен)
Это то, что клиенты набирают в браузере для просмотра товаров:
- `rosek.ru` - основной домен
- `shop.rosek.ru` - поддомен (если хотите)
- `rosek.company.ru` - поддомен на домене компании

**Где находится:** На хостинге (сервер с вашими HTML/PHP файлами)

**Где получить:** Покупаете домен у регистратора (REG.RU, Timeweb, Beget и т.д.) и привязываете к хостингу

#### 2. Адрес 1С (IP или домен сервера 1С)
Это адрес компьютера/сервера где установлена программа 1С:
- `http://192.168.1.100:8080/accounting/hs/orders/create` - если 1С в офисе (внутренний адрес)
- `http://85.123.45.67:8080/accounting/hs/orders/create` - если 1С в офисе с белым IP
- `http://1c.company.ru/base/hs/orders/create` - если 1С на арендованном сервере

**Где находится:** На вашем сервере/компьютере с установленной 1С

**Где получить:** Это настраивает системный администратор или тот, кто установил 1С

### Как они работают вместе

```
Клиент → открывает rosek.ru (сайт на хостинге)
           ↓
         оформляет заказ
           ↓
Сайт → отправляет заказ → http://85.123.45.67:8080 (ваша 1С)
           ↓
         1С создает документ "Заказ покупателя"
```

**Важно:** Сайт и 1С - это РАЗНЫЕ серверы! Сайт должен "дотянуться" до 1С через интернет.

### Варианты размещения 1С

#### Как узнать где находится ваша 1С?

**Спросите у того, кто работает с 1С:**

1. **Если 1С запускается с ярлыка на компьютере** → 1С в офисе (на локальном компьютере или сервере)
   - Спросите у системного администратора или программиста 1С
   - Они скажут адрес сервера (например: `192.168.1.100`)

2. **Если 1С открывается через браузер** → 1С в облаке или на арендованном сервере
   - Посмотрите адрес в браузере (например: `https://1c.company.ru`)
   - Это и есть адрес вашей 1С

3. **Если используете 1С:Fresh, 1С:Облако** → 1С в облаке
   - Обратитесь в поддержку провайдера облачной 1С
   - Они дадут адрес API для интеграции

**Простой способ проверить:**
- Откройте 1С
- Посмотрите в заголовок окна или спросите у бухгалтера/программиста 1С
- Если не знаете - обратитесь к тому, кто настраивал 1С в вашей компании

#### Варианты размещения 1С

**Вариант 1: 1С в офисе (на локальном компьютере)**

Проблема: Адрес `192.168.1.100` - это внутренний адрес офиса, из интернета его не видно!

**Решения (выберите одно):**

**А) Получить белый IP (самое простое)**
1. Позвоните интернет-провайдеру и закажите "белый статический IP"
2. Настройте проброс портов на роутере (обратитесь к системному администратору)
3. Используйте адрес: `http://ваш-белый-IP:8080/база/hs/orders/create`

**Б) Использовать VPN-туннель (бесплатно, но сложнее)**
1. Установите на сервер с 1С программу для VPN (например: ngrok, localtunnel)
2. Получите публичный адрес типа: `https://abc123.ngrok.io`
3. Используйте этот адрес в config.ini
4. Минус: адрес может меняться при перезапуске

**В) Перенести 1С на облачный сервер (рекомендуется)**
1. Арендуйте VPS сервер или используйте 1С:Fresh
2. Установите 1С на сервер
3. Получите постоянный адрес: `http://1c.company.ru`
4. Плюс: доступ к 1С из любого места, резервные копии

**Г) Использовать сервис динамического DNS (если белый IP динамический)**
1. Зарегистрируйтесь на No-IP.com или DynDNS
2. Получите адрес типа: `http://mycompany.ddns.net`
3. Настройте автообновление IP

**Что такое белый IP простыми словами:**
- Серый IP (192.168.x.x) - это как квартира в доме, видна только внутри дома
- Белый IP (85.123.45.67) - это как адрес дома, виден со всего города
- Чтобы сайт из интернета мог подключиться к 1С в офисе, нужен белый IP

**Вариант 2: 1С на арендованном сервере (РЕКОМЕНДУЕТСЯ)**

Арендуете сервер с 1С у провайдера, получаете готовый адрес:
- `http://1c.your-company.ru/base/hs/orders/create`

Этот адрес доступен из интернета сразу, ничего настраивать не нужно!

**Вариант 3: 1С в облаке (1С:Fresh и т.д.)**

Провайдер облачной 1С дает готовый адрес API.

### Что указывать в config.ini

Не важно где находится ваш сайт (домен, поддомен, любой хостинг) - главное правильно указать адрес 1С:

```ini
# Если 1С в офисе с белым IP
ONEC_API_URL = "http://85.123.45.67:8080/accounting/hs/orders/create"

# Если 1С на арендованном сервере
ONEC_API_URL = "http://1c.company.ru/base/hs/orders/create"
```

**Домен сайта не влияет на подключение к 1С!** Сайт может быть на любом домене - он все равно подключится к 1С по указанному адресу.

---

# ШАГ 1: Настройка 1С (для работника 1С)

## 1.1. Создать HTTP-сервис

1. Откройте 1С в режиме Конфигуратора
2. В дереве конфигурации найдите раздел "HTTP-сервисы"
3. Правой кнопкой мыши → Добавить
4. Заполните:
   - Имя: `orders`
   - Корневой URL: `orders`
   - Описание: `Прием заказов с сайта`
5. Поставьте галочку "Базовая аутентификация"

## 1.2. Создать URL шаблон для приема заказов

1. В HTTP-сервисе `orders` создайте URL шаблон
2. Правой кнопкой → Добавить
3. Заполните:
   - Имя: `create`
   - Шаблон: `create`
   - HTTP метод: `POST`
4. Создайте метод с именем `CreateOrder`

## 1.3. Вставить код метода CreateOrder

Скопируйте и вставьте этот код в метод `CreateOrder`:

```1c
Функция CreateOrderПОСТ(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.Заголовки.Вставить("Content-Type", "application/json");
    
    Попытка
        // Читаем JSON
        ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку(КодировкаТекста.UTF8);
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(ТелоЗапроса);
        ДанныеЗаказа = ПрочитатьJSON(ЧтениеJSON);
        ЧтениеJSON.Закрыть();
        
        // Создаем заказ
        НовыйЗаказ = Документы.ЗаказПокупателя.СоздатьДокумент();
        НовыйЗаказ.Дата = ТекущаяДата();
        
        // Находим/создаем контрагента
        Если ДанныеЗаказа.clientType = "legal" Тогда
            Контрагент = НайтиИлиСоздатьКонтрагента(
                ДанныеЗаказа.clientInfo.companyName,
                ДанныеЗаказа.clientInfo.inn,
                ДанныеЗаказа.clientInfo.kpp,
                Истина
            );
        Иначе
            Контрагент = НайтиИлиСоздатьКонтрагента(
                ДанныеЗаказа.clientInfo.fullName,
                "",
                "",
                Ложь
            );
        КонецЕсли;
        
        НовыйЗаказ.Контрагент = Контрагент;
        НовыйЗаказ.ТелефонКонтрагента = ДанныеЗаказа.clientInfo.phone;
        НовыйЗаказ.EmailКонтрагента = ДанныеЗаказа.clientInfo.email;
        
        // Добавляем товары
        Для Каждого Товар Из ДанныеЗаказа.items Цикл
            НоваяСтрока = НовыйЗаказ.Товары.Добавить();
            Номенклатура = НайтиНоменклатуруПоАртикулу(Товар.article);
            
            Если Номенклатура <> Неопределено Тогда
                НоваяСтрока.Номенклатура = Номенклатура;
                НоваяСтрока.Количество = Товар.quantity;
                НоваяСтрока.Цена = Товар.price;
                НоваяСтрока.Сумма = Товар.totalPrice;
            КонецЕсли;
        КонецЦикла;
        
        // Сохраняем
        НовыйЗаказ.Записать(РежимЗаписиДокумента.Проведение);
        
        // Отправляем ответ
        РезультатJSON = Новый Структура;
        РезультатJSON.Вставить("success", Истина);
        РезультатJSON.Вставить("orderId", Строка(НовыйЗаказ.Ссылка.УникальныйИдентификатор()));
        РезультатJSON.Вставить("orderNumber", НовыйЗаказ.Номер);
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
        Ответ.УстановитьТелоИзСтроки(ЗаписьJSON.Закрыть());
        
    Исключение
        РезультатJSON = Новый Структура;
        РезультатJSON.Вставить("success", Ложь);
        РезультатJSON.Вставить("error", ОписаниеОшибки());
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
        
        Ответ.КодСостояния = 500;
        Ответ.УстановитьТелоИзСтроки(ЗаписьJSON.Закрыть());
    КонецПопытки;
    
    Возврат Ответ;
    
КонецФункции

Функция НайтиИлиСоздатьКонтрагента(Наименование, ИНН, КПП, ЭтоЮрЛицо)
    Запрос = Новый Запрос;
    
    Если ЭтоЮрЛицо И НЕ ПустаяСтрока(ИНН) Тогда
        Запрос.Текст = "ВЫБРАТЬ ПЕРВЫЕ 1 Контрагенты.Ссылка ИЗ Справочник.Контрагенты ГДЕ Контрагенты.ИНН = &ИНН";
        Запрос.УстановитьПараметр("ИНН", ИНН);
    Иначе
        Запрос.Текст = "ВЫБРАТЬ ПЕРВЫЕ 1 Контрагенты.Ссылка ИЗ Справочник.Контрагенты ГДЕ Контрагенты.Наименование = &Наименование";
        Запрос.УстановитьПараметр("Наименование", Наименование);
    КонецЕсли;
    
    Результат = Запрос.Выполнить();
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    КонецЕсли;
    
    НовыйКонтрагент = Справочники.Контрагенты.СоздатьЭлемент();
    НовыйКонтрагент.Наименование = Наименование;
    Если ЭтоЮрЛицо Тогда
        НовыйКонтрагент.ИНН = ИНН;
        НовыйКонтрагент.КПП = КПП;
    КонецЕсли;
    НовыйКонтрагент.Записать();
    
    Возврат НовыйКонтрагент.Ссылка;
КонецФункции

Функция НайтиНоменклатуруПоАртикулу(Артикул)
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ ПЕРВЫЕ 1 Номенклатура.Ссылка ИЗ Справочник.Номенклатура ГДЕ Номенклатура.Артикул = &Артикул";
    Запрос.УстановитьПараметр("Артикул", Артикул);
    
    Результат = Запрос.Выполнить();
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    КонецЕсли;
    
    Возврат Неопределено;
КонецФункции
```

## 1.4. Создать URL шаблон для проверки

1. В HTTP-сервисе `orders` создайте еще один URL шаблон
2. Правой кнопкой → Добавить
3. Заполните:
   - Имя: `ping`
   - Шаблон: `ping`
   - HTTP метод: `GET`
4. Создайте метод с именем `ping`

## 1.5. Вставить код метода ping

```1c
Функция pingGET(Запрос)
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.Заголовки.Вставить("Content-Type", "application/json");
    
    РезультатJSON = Новый Структура;
    РезультатJSON.Вставить("status", "ok");
    
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
    Ответ.УстановитьТелоИзСтроки(ЗаписьJSON.Закрыть());
    
    Возврат Ответ;
КонецФункции
```

## 1.6. Создать пользователя для сайта

1. Откройте 1С:Предприятие (не конфигуратор)
2. Перейдите в "Администрирование" → "Пользователи"
3. Создайте нового пользователя:
   - Имя: `WebService`
   - Придумайте надежный пароль (запишите его!)
   - Права: дайте права на создание документов "Заказ покупателя"

## 1.7. Опубликовать HTTP-сервис

1. Сохраните конфигурацию (Ctrl+S)
2. Обновите конфигурацию базы данных (Конфигурация → Обновить конфигурацию базы данных)
3. Перезапустите 1С:Предприятие

## 1.8. Узнать URL сервиса

URL вашего HTTP-сервиса будет иметь вид:
```
http://[адрес-сервера]:[порт]/[имя-базы]/hs/orders/create
```

Примеры:
- Локальный: `http://localhost/accounting/hs/orders/create`
- Сетевой: `http://192.168.1.100:8080/accounting/hs/orders/create`
- Внешний: `http://1c.company.ru/base/hs/orders/create`

## 1.9. Проверить работу

Откройте командную строку и выполните:
```bash
curl -u WebService:ваш-пароль http://адрес-сервера/база/hs/orders/ping
```

Должен вернуться ответ: `{"status":"ok"}`

## 1.10. Настроить автоматическое обновление цен (опционально)

Если нужно автоматически обновлять цены на сайте из 1С:

### 1.10.1. Создать процедуру

1. Откройте конфигуратор 1С
2. Создайте общий модуль или обработку
3. Добавьте процедуру:

```1c
Процедура ВыгрузитьЦеныНаСайт()
    МассивЦен = Новый Массив;
    
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ Номенклатура.Артикул, Номенклатура.Цена ИЗ Справочник.Номенклатура ГДЕ Номенклатура.ВыгружатьНаСайт = ИСТИНА";
    
    Результат = Запрос.Выполнить();
    Выборка = Результат.Выбрать();
    
    Пока Выборка.Следующий() Цикл
        Элемент = Новый Структура;
        Элемент.Вставить("article", Выборка.Артикул);
        Элемент.Вставить("price", Выборка.Цена);
        МассивЦен.Добавить(Элемент);
    КонецЦикла;
    
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    Данные = Новый Структура;
    Данные.Вставить("prices", МассивЦен);
    ЗаписатьJSON(ЗаписьJSON, Данные);
    JSON = ЗаписьJSON.Закрыть();
    
    // ВАЖНО: Замените на домен сайта и токен из config.ini (получите у владельца)
    Соединение = Новый HTTPСоединение("ваш-домен.ru", 443, , , , 30, Новый ЗащищенноеСоединениеOpenSSL);
    Запрос = Новый HTTPЗапрос("/api/update-prices.php");
    Запрос.Заголовки.Вставить("Content-Type", "application/json");
    Запрос.Заголовки.Вставить("Authorization", "Bearer ваш-токен-из-config-ini");
    Запрос.УстановитьТелоИзСтроки(JSON, КодировкаТекста.UTF8);
    
    Попытка
        Ответ = Соединение.ОтправитьДляОбработки(Запрос);
        Если Ответ.КодСостояния = 200 Тогда
            Сообщить("Цены обновлены на сайте!");
        Иначе
            Сообщить("Ошибка: " + Ответ.КодСостояния);
        КонецЕсли;
    Исключение
        Сообщить("Ошибка: " + ОписаниеОшибки());
    КонецПопытки;
КонецПроцедуры
```

### 1.10.2. Создать регламентное задание

1. В 1С:Предприятие перейдите в "Администрирование" → "Регламентные задания"
2. Создайте новое задание:
   - Имя: "Выгрузка цен на сайт"
   - Метод: `ВыгрузитьЦеныНаСайт`
   - Расписание: каждый час (или по вашему выбору)
3. Сохраните

Теперь цены будут автоматически обновляться на сайте!

## 1.11. Передать данные владельцу сайта

Сообщите владельцу сайта три параметра:
1. **URL**: `http://адрес-сервера/база/hs/orders/create`
2. **Логин**: `WebService`
3. **Пароль**: ваш-пароль

Если настроили обновление цен (шаг 1.10), попросите у владельца:
- **Домен сайта**: например `rosek.ru`
- **Токен из config.ini**: например `rosek-2026-secret-xyz789`

---

# ШАГ 2: Подготовка сайта (для владельца сайта)

## 2.1. Добавить QR-код для оплаты

1. Получите QR-код от вашего банка для приема платежей
2. Сохраните изображение как `qr-payment.png`
3. Положите файл в папку `images/`

Путь должен быть: `images/qr-payment.png`

## 2.2. Настроить config.ini

1. Откройте файл `config.ini` в корне сайта
2. Замените значения:

```ini
; Придумайте любой сложный токен (буквы, цифры, дефисы)
; Например: rosek-2026-secret-xyz789-abc123
API_TOKEN = "ваш-токен-здесь"

; Эти данные вам дал работник 1С (из шага 1.10)
ONEC_API_URL = "http://адрес-сервера/база/hs/orders/create"
ONEC_USERNAME = "WebService"
ONEC_PASSWORD = "пароль-от-1с"
ONEC_RETRY_ATTEMPTS = 3
```

**Пример заполненного config.ini:**
```ini
API_TOKEN = "rosek-2026-secret-xyz789-abc123"
ONEC_API_URL = "http://192.168.1.50:8080/accounting/hs/orders/create"
ONEC_USERNAME = "WebService"
ONEC_PASSWORD = "MyPassword123"
ONEC_RETRY_ATTEMPTS = 3
```

## 2.3. Проверить файлы

Убедитесь что у вас есть все файлы:
- ✅ `config.ini` - настроен
- ✅ `images/qr-payment.png` - добавлен
- ✅ `api/` - папка с PHP скриптами
- ✅ `data/` - папка с товарами
- ✅ `orders/` - папка для заказов (пустая)
- ✅ `.htaccess` - настройки Apache

---

# ШАГ 3: Загрузка на хостинг

## 3.1. Подключиться к хостингу

Используйте FTP/SFTP клиент (FileZilla, WinSCP) или файловый менеджер хостинга.

**Данные для подключения** (получите у хостинг-провайдера):
- Хост: ftp.ваш-домен.ru
- Логин: ваш-логин
- Пароль: ваш-пароль
- Порт: 21 (FTP) или 22 (SFTP)

## 3.2. Загрузить файлы

1. Подключитесь к хостингу
2. Перейдите в корневую папку сайта (обычно `public_html` или `www`)
3. Загрузите ВСЕ файлы проекта:
   - Все HTML файлы
   - Папки: `api/`, `category/`, `css/`, `data/`, `documents/`, `images/`, `js/`, `orders/`
   - Файлы: `config.ini`, `.htaccess`, `README.md`

**ВАЖНО:** Загружайте именно файлы, а не папку проекта целиком!

## 3.3. Проверить требования хостинга

Убедитесь что на хостинге установлено:
- ✅ PHP 7.4 или выше
- ✅ Модуль PHP cURL (для отправки в 1С)
- ✅ Apache с mod_rewrite (или Nginx)

Обычно это уже есть на любом хостинге.

## 3.4. Настроить права доступа

Через FTP клиент или панель хостинга установите права:

**Для папок:**
- `orders/` - права 755 (rwxr-xr-x)
- `data/` - права 755 (rwxr-xr-x)

**Для файлов:**
- `config.ini` - права 644 (rw-r--r--)
- Все остальные файлы - права 644

В FileZilla: правой кнопкой на папку → Права доступа к файлу

## 3.5. Проверить работу сайта

1. Откройте в браузере: `https://ваш-домен.ru`
   - Должна открыться главная страница

2. Откройте: `https://ваш-домен.ru/api/health.php`
   - Должно показать: `{"status":"ok","message":"Сервер работает"}`

3. Откройте: `https://ваш-домен.ru/category/catalog.html`
   - Должны отображаться товары

## 3.6. Проверить интеграцию с 1С

1. Оформите тестовый заказ на сайте
2. Проверьте что заказ появился в 1С
3. Проверьте что файл заказа создался в папке `orders/`

Если заказ не появился в 1С:
- Проверьте URL в `config.ini`
- Проверьте логин/пароль
- Посмотрите файл заказа в папке `orders/` - там будут данные

---

# Решение проблем

## Заказы не приходят в 1С

1. Проверьте `config.ini`:
   - URL правильный?
   - Логин/пароль правильные?

2. Проверьте папку `orders/` на хостинге:
   - Есть ли там файлы заказов?
   - Откройте файл - там все данные заказа

3. Проверьте доступность 1С:
   ```bash
   curl -u WebService:пароль http://адрес-1с/база/hs/orders/ping
   ```

## Не обновляются цены

1. Проверьте токен в `config.ini` и в коде 1С - они должны совпадать
2. Проверьте что в 1С у товаров заполнено поле "Артикул"
3. Проверьте что артикулы в 1С совпадают с артикулами в файлах `data/*.json`

## Ошибка 500 на сайте

1. Проверьте что PHP 7.4+ установлен
2. Проверьте права на папки `orders/` и `data/` (должно быть 755)
3. Проверьте логи PHP на хостинге

## Товары не отображаются

1. Проверьте что файлы из папки `data/` загружены на хостинг
2. Проверьте права на файлы (должно быть 644)
3. Откройте в браузере: `https://ваш-домен.ru/data/products-avr.json`
   - Должен открыться JSON с товарами

---

# Контакты

Email: zakaz@rosek.tech
Телефон: 8 (800) 55-11-052
